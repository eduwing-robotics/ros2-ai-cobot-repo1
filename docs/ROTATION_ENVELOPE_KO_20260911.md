# 성공 방향 보정과 전체 회전 한도 정합성 — 2026-09-11

실행 b6942736-334c-4cb9-9f68-7b80dcd2a213는 25개 촬영·VRM 정밀 보정을 통과한 뒤 PM-01 rotation 96.111deg exceeds 95.000deg로 계획 단계에서 실패했다.

## 수정 근거

기존 성공 방향 검사는 잡기·배치 각각 기준 C에서 ±15도를 허용하지만 GPU·PM 전체 회전은 95도, IND 양의 전체 회전은 185도로 제한했다. 기준 90도 이동은 양 끝 보정까지 최대120도, 기준180도 양의 이동은 최대210도가 될 수 있어 유효한 끝점끼리도 거부됐다. VRM은 이미 같은 근거로120도, CAP01 양의 분할 이동은210도를 사용하고 있었다.

GPU·PM 전체 TCP 회전 한도를120도, IND는210도로 맞췄다. IND의 스냅샷 검사·계획 검사·양의 분할 경로 검사도 일치시켰다. 성공한 끝점 방향 ±15도, 반대 방향 거부, IND 양의 회전, 각 분할 회전 최대60도, 개별 관절 변화95도, J6 범위 및 관절 소프트 리밋 여유10도 검사는 유지한다. 이는 개별 관절 한도를 올리거나 실패 시 반대 방향으로 바꾸는 수정이 아니다.

설정 파일 해시와 런처 검토 기록을 함께 갱신했다. 기존 조립 레시피 식별자는 유지하며 위치·높이·그리퍼 조건은 바꾸지 않았다. 실행 중 작업이 없는 상황에서 적용하며 기존 실패 기록은 보존한다.

## 검증

- 최종 전체 회귀 테스트859개 통과(41.33초). API 서버 재시작으로 반영 완료.

- 실패 장면을 회귀 fixture로 보존. 이전95도 정책에서 동일 오류를 재현하고 수정 정책에서 일반20개 계획과 독립 실행기 검증 통과.
- PM 실제96.111도, GPU/PM120도 및 IND190/210도 경계 사례 검증. 방향 허용 범위 밖은 거부, IND 양의 회전은60도 이하로 분할.
- 실제 제어기의 조회 RPC만 허용한 진단: GetJointSoftLimitDeg, GetSafetyStopState, GetInverseKinRef. launcher/step_operation 잠금 사용. Move/Set/그리퍼 요청 금지.
- 최신 실패 자료 일반20개: 433개 경유점 통과, 최대 관절 변화93.254도, J6 -18.888~100.980도. 최종 설정의 잡기/배치 좌표가 검증한 후보 계획과 동일함을 확인.
- 보관된 SMDView 측정 자료(0bf5780c-d6bf-4d01-b949-1365c6614164)로 SMD5개 계획 및 실제 제어기90개 경유점 통과. 최대 관절 변화41.791도, J6 -4.689~100.369도. 현재 SMD 촬영 성공을 의미하지 않는다.
- 설정 무결성 검사 통과. 증거 runtime/rotation_plan_fix_20260911/의 non_smd_preflight.log, smd_preflight.log, final_plan.json, tests.log.

진단용 과거 계획을 실행하지 않았다. 실제 다음 실행은 현재 영상으로 새 계획을 만들고 다시 제어기 검사를 해야 한다. 로봇·그리퍼 동작, 복구 명령은 보내지 않았다. 실제 전체 물리 조립 성공은 아직 검증하지 않았다.
