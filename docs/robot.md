# FR5 로봇 작업 기록

## 2026-08-13 — 소형 부품 안전 접근 경로 dry-run

- 검출된 부품 상면 Base XYZ는 `[-313.664, -58.659, -10.061] mm`였다.
- Base +Z `100 mm` 접근 목표는 `[-313.664, -58.659, 89.939] mm`다.
- 현재 TCP 방향 `[180.0, 0.0, 89.999]°`를 유지하는 3단계 경로를 검증했다:
  수직 상승 `Z=189.939 mm` → 안전 높이 수평 이동 → `Z=89.939 mm` 저속 접근.
- 총 목표 거리 약 `106.5 mm`, 수평 `20%`, 수직 `8%` 설정이었다.
- `--dry-run`만 실행했으며 이동·표면 하강·그리퍼 작동은 없었다.
- 당시 Tool 1, 정지, 오류 없음이지만 `robot_mode=1` 수동 모드였다. 실제 이동은
  AUTO mode 0과 작업 공간 확인 후 별도 승인으로 진행한다.

## 현재 기준 상태

- 로봇: FAIRINO FR5
- End Effector: DH Robotics PGEA-100-40 및 제작 핑거
- 활성 Tool: `toolcoord1`
- 최근 시험 TCP 후보: `[-2, -2, 157, 0, 0, 0] mm/deg`
- Hand-Eye 계산에는 Flange pose를 사용하고 MoveCart에는 활성 TCP pose를
  사용한다. TCP와 Camera extrinsic을 중복 적용하지 않는다.
- 실제 이동은 dry-run 확인 후에만 허용하며, `Ctrl+C`가 이미 전달된 FR5
  동작을 취소한다고 가정하지 않는다.

## 2026-08-12 — 후보 보정 실제 접근 측정

- marker ID 8 중심의 Base +Z 100 mm 위로 후보 보정값을 사용해 접근했다.
- 초기 육안 보고값 두 개는 측정 정확도가 불확실해 예비 관찰로 강등했다.
- 신뢰 측정은 접근 후 Tool 기준 데카르트 조그를 1 mm/0.5 mm 단위로 적용해
  중심을 맞추는 방식으로 통일했다.
- 저장된 관측 좌표에서 첫 유효 XY 보정은 Tool `X -2.5 mm`, `Y -2.5 mm`다.
- 수동 하강량은 XY 중심을 확인하기 위한 것이며 Z 오차로 기록하지 않는다.
- 동일 방식의 다른 관측 자세 결과가 모이기 전에는 TCP나 코드 correction을
  수정하지 않는다.
- 두 번째 유효 측정은 Tool `X -5.5 mm`, `Y 0 mm`였다. 첫 번째
  `[-2.5,-2.5] mm`와 달라 고정 correction 적용을 계속 보류한다.
- 첫 관측 자세로 정확히 복귀한 반복 시험에서는 다시 Tool
  `[-2.5,-2.5] mm`가 측정됐다. 수동 조그 측정은 재현되며 관측 자세가
  바뀔 때 필요한 보정이 달라지는 것으로 확정했다.
## 2026-08-12 — RGB-D 검출 물체 안전 접근 실동작

- 높이 10 mm 등록 물체의 검출 Base XY와 ChArUco 작업대 평면을 이용해
  물체 윗면 100 mm 위 접근점을 계산했다.
- 명령 목표 TCP/Base: `[-312.758, -62.492, 96.468] mm`.
- 실제 도착 TCP/Base: `[-312.757, -62.494, 96.467] mm`.
- 명령 대비 축 오차는 약 `[+0.001, -0.002, -0.001] mm`로 컨트롤러 도착
  정밀도는 정상이다.
- 도착 후 `robot_motion_done=1`, `main_error_code=0`, `collision_err=0` 확인.
- 물체 표면 하강 및 그리퍼 동작은 수행하지 않았다.
### 테스트 물체 근접 XY 수동 보정

- 물체 윗면 예상 높이까지 접근 후 충돌 없이 약 50 mm 상승하여 중심을 확인했다.
- 사용자가 Tool 기준 `X +1.5 mm, Y -4.0 mm`를 수동 적용했고, 열린/파지
  중심이 물체 중심과 육안 일치한다고 확인했다.
- 보정 전 TCP XY `[-312.756, -62.494] mm`, 보정 후 실제 TCP XY
  `[-316.774, -61.493] mm`로 Base 기준 변화는 약 `[-4.019, +1.001] mm`였다.
- Tool 조그 입력과 실제 Base 변화는 현재 Tool orientation 및 조그 분해능의
  영향을 받으므로 Tool 값을 Base 고정 오프셋으로 직접 저장하지 않는다.
- 현재 TCP Z `46.442 mm`, 이동 완료·오류 없음·충돌 신호 없음 확인.
### RGB-D 테스트 물체 첫 파지 성공

- 근접 중심 보정 후 그리퍼를 완전히 연 상태에서 Base Z로 단계 하강했다.
- 하강량: `-35 mm` 후 `-5 mm × 5`, 총 `-60 mm`.
- 핑거와 바닥 간 육안 여유 약 `1~2 mm`에서 하강을 중단하고 저속/저력으로
  그리퍼를 닫았다.
- Base Z +10 mm 시험 상승 시 물체가 함께 들어 올려져 파지 성공을 확인했다.
- 성공 후 TCP/Base: `[-316.761, -61.492, 36.432] mm`, orientation
  `[180.000, 0.000, 89.999] deg`.
- `robot_motion_done=1`, `grip_motion_done=1`, `gripperfaultnum=0`,
  `main_error_code=0`, `collision_err=0` 확인.

## 2026-08-13 — 최신 부품 좌표 연동 및 수직 속도 범위 조정

- `move_object_approach.py`가 `small_part_last.json`의 최신 Base 좌표를 읽는
  `--target-file` 방식을 지원하도록 변경했다.
- 오래된 검출 결과의 재사용을 막기 위해 기본 유효시간을 120초로 제한했다.
- 수직 상승/접근 속도 허용 범위를 기존 최대 15%에서 최대 25%로 높였고,
  기본값은 15%, 현장 권장값은 15~20%로 유지했다.
- 최신 검출 결과로 100 mm 안전 접근점을 `[-321.980,-71.312,90.037] mm`로
  계산했으며 수직 속도 20%의 3단계 경로를 dry-run 검증했다.
- 이 검증에서는 로봇 이동 명령을 보내지 않았다.

### 직사각형 부품 방향 정렬 기능 추가

- `move_object_approach.py`에 `--align-part`와 `--gripper-axis` 옵션을 추가했다.
- 부품 장축과 Tool X/Y 축의 Base XY 투영을 비교하고, 직사각형의 180° 대칭을
  이용해 `-90~+90°` 범위의 최소 회전만 계산한다.
- 회전은 목표 상공으로 내려가기 전 safe Z에서 별도 단계로 수행한다.
- 각도 계산 단위시험은 통과했으며 실제 로봇 이동은 수행하지 않았다.
- Tool X와 Tool Y 중 실제 핑거 장축에 해당하는 축은 safe-height dry-run 및
  육안 시험으로 확정해야 한다.

### 소형 부품 TCP 중심 반복 오차 보정

- 서로 다른 부품 위치에서 측정한 Tool 보정 6회:
  `(-2.0,-1.0)`, `(-2.5,-1.0)`, `(-1.0,-1.5)`, `(-2.0,-2.0)`,
  `(-2.5,-2.5)`, `(-2.3,-1.3) mm`.
- 평균 Tool 중심 보정 `X=-2.05 mm, Y=-1.55 mm`를 기본값으로 적용했다.
- 보정값을 Base XY에 직접 더하지 않고 목표 Tool 회전행렬로 Base 좌표에
  변환하도록 구현했다. 대각선 시험 자세에서는 Base 보정
  `[+0.392,-2.540,0.000] mm`로 계산됐다.
- `--no-center-correction`으로 보정을 끌 수 있고,
  `--tool-correction-x-mm`, `--tool-correction-y-mm`로 시험값을 변경할 수 있다.
- XY 30%, 수직 20% 경로를 dry-run 검증했으며 로봇은 움직이지 않았다.

#### 다중 ChArUco 코너 좌표 개선 후 Y 잔차 보정

- 개선된 다중 코너 homography를 적용한 대각선 시험에서 Tool X는 중심과
  일치했고 Tool Y는 `-1.0 mm` 추가 이동이 필요했다.
- 기본 Tool 보정을 `X=-2.05 mm, Y=-1.55 mm`에서
  `X=-2.05 mm, Y=-2.55 mm`로 조정했다.
- 다른 위치·각도에서 Y 잔차가 반복되는지 추가 확인이 필요하며, 실제 이동은
  수행하지 않았다.

### 부품 방향 정렬 회전 속도 분리

- 부품 방향 정렬 단계가 수직 접근 속도를 공유하던 구조를 분리했다.
- 새 옵션 `--rotation-speed-percent`를 추가했으며 기본 30%, 허용 범위는
  1~40%다.
- XY 이동은 `--speed-percent`, 수직 이동은 `--descent-speed-percent`,
  안전 높이 회전은 `--rotation-speed-percent`로 각각 독립 설정한다.
- 실제 로봇 이동은 수행하지 않았다.

### 이동 속도 사용자 조정 상한 확장

- 사용자 요청에 따라 소형 부품 접근 코드의 XY, 수직, 회전 속도 허용 상한을
  모두 50%로 확장했다.
- 기본값은 XY 20%, 수직 15%, 회전 30%로 유지해 기존 명령의 기본 동작은
  바뀌지 않는다.
- 실제 로봇 이동은 수행하지 않았다.

### Base Z 100 mm 하강·정지 시험 도구

- 현재 TCP의 XY와 ABC를 유지하고 Base Z로 최대 100 mm만 하강한 뒤 정지하는
  `run_vertical_test.sh`를 추가했다. 그리퍼 명령은 보내지 않는다.
- 실제 하강에는 `--execute --confirm-descent` 이중 확인이 필요하다.
- 최신 검출 부품과 현재 TCP의 XY 차이가 기본 10 mm를 넘으면 하강을 차단한다.
- 구현 당시 현재 TCP와 부품 XY 차이가 204.4 mm로 확인돼 하강이 정상
  차단됐다. 먼저 부품 위 안전 접근을 완료해야 한다.
- 실제 로봇 이동은 수행하지 않았다.

### 소형 부품 수동 Pick & Place 전체 사이클 성공

- 사용자가 비전 접근 위치에서 총 105 mm 하강한 뒤 그리퍼를 닫아 부품을
  파지하고, 상승 후 같은 위치로 재하강해 그리퍼를 열어 깔끔하게 배치했다.
- 검출·정렬·중심 보정·하강·파지·상승·배치의 전체 기계 동작 가능성을
  실제로 확인했다.

### 같은 위치 자동 파지·배치 시험 코드

- 현재 파지 높이에서 `닫기 → Base Z +20 mm → 원래 Z → 열기 → Base Z
  +20 mm`를 수행하는 `run_grasp_place_cycle.sh`를 추가했다.
- 현재 그리퍼 조회 결과는 `GetGripperCurPosition(1) = 0,0,5`였다. 다만 이
  시점의 5가 열림/닫힘 중 어느 목표였는지 명확하지 않아 자동 코드에서
  open/close position 기본값을 제거하고 두 값을 필수 입력으로 변경했다.
- 현재 dry-run: TCP `[-391.308,81.471,-14.460] mm`, 검출 부품
  `[-391.524,84.739,-9.467] mm`, XY 차이 3.275 mm.
- 실제 실행에는 `--execute --confirm-gripper --confirm-cycle` 세 가지 확인이
  모두 필요하다. dry-run에서 로봇과 그리퍼는 움직이지 않았다.

#### PGEA-100-40 Web 설정 반영

- 사용자가 알려준 구성: 장치 ID 1, 위치 범위 0~100, 개폐 속도 50%,
  개폐 토크 1%, 최대시간 3000 ms.
- FAIRINO 명령 서버 기본값을 속도 50, 토크 1, 최대시간 3000 ms로 변경했다.
- 기존 서버 코드가 `gripper_maxtime` 파라미터를 읽고도 SDK에 항상 30000 ms를
  전달하던 부분을 수정해 실제 파라미터값을 전달하도록 했다.
- 변경된 `fairino_hardware_v3_9_7` 빌드를 완료했으며 서버 재시작 후 적용된다.
- 사용자가 위치 방향을 `닫힘=5`로 확인했다. 위치 범위에 따라 자동 사이클
  기본값을 `닫힘=5`, `열림=100`으로 확정했다.

### 한 명령 자동 Pick & Place 동일 위치 시험

- `run_full_pick_place_same_spot.sh`를 추가해 다음 단계를 한 명령으로 연결했다:
  부품 재검출 → 그리퍼 열기 → 안전 접근 → 100 mm 하강 → 추가 5 mm 하강
  → 닫기 → 50 mm 상승 → 같은 Z로 재하강 → 열기 → 50 mm 후퇴.
- 각 하위 단계가 비정상 종료되면 즉시 전체 시퀀스를 중단한다.
- 실제 실행에는 `--execute --confirm-full-cycle` 이중 확인이 필요하다.
- 통합 dry-run에서 셀 `(4,0)`, Base 부품 위치
  `[-381.288,-127.963,-9.286] mm`, 접근 목표
  `[-381.482,-131.229,90.714] mm`를 계산했다.
- 실행 중 명령 서버 파라미터가 속도 50, 토크 1, 최대시간 3000 ms로 실제
  적용된 것을 `ros2 param get`으로 확인했다.
- dry-run에서는 로봇과 그리퍼가 움직이지 않았다.

#### 통합 사이클 기본 속도 확정

- 일반 XY·수직 이동과 파지 후 상승/재하강 속도: 40%.
- 부품 방향에 맞춘 Tool 회전 속도: 50%.
- 마지막 추가 5 mm 파지 근접 하강 속도: 15%.
- 파지 후 상승 높이: 50 mm.
- 배치 후 후퇴 높이 기본값도 50 mm로 유지했다.
- 마지막 5 mm 속도를 `--grasp-descent-speed-percent`로 일반 수직 속도와
  분리했다. 실제 로봇 이동은 수행하지 않았다.

#### 첫 통합 사이클 완료 감시 timeout 및 복구

- 부품 검출·접근·105 mm 하강·그리퍼 닫기·50 mm 상승까지 정상 성공했다.
- 상승 후 `MoveCart` 서비스가 블로킹으로 완료된 뒤 응답해
  `robot_motion_done=0` 순간을 감시 코드가 보지 못했고 30초 timeout이 발생했다.
- 정지 상태뿐 아니라 목표 XYZ 1 mm 이내 도착을 함께 확인하도록 완료 판정을
  수정했다.
- timeout 시점 상태: TCP `[-325.760,-66.712,35.007] mm`, 그리퍼 위치 5,
  `grip_motion_done=2`, 로봇/충돌/그리퍼 오류 0으로 정상 파지·상승 상태였다.
- 현재 상승 상태에서만 `50 mm 재하강 → 열기(100) → 50 mm 후퇴`하는
  `--resume-after-lift` 복구 옵션을 추가하고 dry-run 검증했다.

## 2026-08-13 — FAIRINO ROS 2 워크스페이스 KSMC 독립 구성

- `KSMC/robot_ws`를 생성하고 검증된 `fairino_msgs`,
  `fairino_hardware_v3_9_7`, `fairino_description`, FR5 MoveIt 설정을 로컬
  vendor 의존성으로 구성했다.
- Jazzy에서 빌드해 `KSMC/robot_ws/install`과 `ros2_cmd_server` 생성을 확인했다.
- 수업용 `~/fr5_jazzy_test_ws`를 source하지 않은 깨끗한 셸에서도 FAIRINO
  메시지·하드웨어 패키지와 명령 서버가 KSMC 경로에서 인식되는 것을 확인했다.
- 캘리브레이션 실행 스크립트는 이제 `KSMC/robot_ws/install/setup.bash`를
  자동 source한다.
- FAIRINO C++ SDK 공식 저장소는 Apache-2.0이나 ROS 2 저장소는 명시적인
  repository-wide LICENSE가 확인되지 않아 `robot_ws/src/vendor/`를 Git에서
  제외했다. Git에는 공식 URL, 고정 커밋, 설치·빌드 스크립트만 포함한다.
- 공식 ROS 2 upstream 고정 커밋:
  `867cb32bc24a73c1e60bef4e6c16762e7357c5e1`.

### Pick & Place 완료 후 ClosePin 자동 복귀

- FR5 SDK의 `GetRobotTeachingPoint(ClosePin)`으로 WebApp 티칭 포인트를
  이름으로 직접 조회할 수 있음을 확인했다.
- 조회된 ClosePin TCP/Base pose:
  `[-215.278, -20.761, 104.748, -180.000, 0.000, 89.999]`, Tool 1, User 0.
- 동일 위치 Pick & Place 시퀀스 마지막에 배치 후 후퇴를 완료하면 ClosePin으로
  자동 복귀하는 6단계를 추가했다.
- 복귀는 현재 위치에서 곧바로 대각 이동하지 않고 `수직 상승 → 안전 높이에서
  수평 이동 및 자세 정렬 → ClosePin Z로 하강` 순서로 실행한다.
- dry-run에서 안전 Z 204.748 mm를 경유하는 3단계 경로를 확인했으며 실제
  로봇 이동은 수행하지 않았다.
- 필요하면 전체 명령에 `--no-return`을 붙여 자동 복귀만 끌 수 있다.

#### 부품 미검출 시 ClosePin fail-safe 복귀

- 파지 전 부품 검출에 기본 15초 timeout을 추가했다.
- 실제 전체 사이클에서 timeout/검출 실패가 발생하면 그리퍼가 비어 있는 파지 전
  단계에서만 `ClosePin`으로 안전 복귀한다.
- 부품을 잡은 뒤 오류가 발생한 경우에는 자동 복귀하지 않고 정지 유지하도록
  단계 정책을 분리했다.
- 현재 부품이 화면에 보이지 않아 ClosePin 복귀를 시도했으나
  `robot_mode=1`(수동)이어서 명령 전 안전검사에서 중단됐다. 실제 이동은 없었다.
- 자동 복귀 실행 전 FR5를 AUTO(`robot_mode=0`)로 전환해야 한다.

### 첫 소형 부품 파지 후 P1 상공 운반

- ChArUco 검정 셀 `(3,3)`의 밝은 6×3.5×2.5 mm 부품을 30프레임 검출했다.
  Base 중심 `[-351.977,-36.891,-9.644] mm`, 최대 흔들림 0.070 mm였다.
- 기존 검증 절차로 부품 위 100 mm 접근, 100 mm 하강, 마지막 5 mm 저속
  하강, 그리퍼 위치 5 닫기, 50 mm 상승을 실행했다.
- 저장된 기판 P1 좌표를 향해 안전 Z 245.859 mm에서 수평 운반한 뒤 P1 표면
  100 mm 상공으로 하강했다.
- 최종 TCP/Base `[74.081,-247.455,95.860] mm`, `robot_motion_done=1`,
  `grip_motion_done=2`, 로봇/충돌 오류 0을 확인했다.
- 부품은 현재 P1 상공에서 파지 중이다. 기판 표면 하강 및 그리퍼 개방은 아직
  실행하지 않았다.

#### 부품 운반 속도 조정

- 파지 후 기판 P1 상공으로 이동하는 수평 운반 속도 기본값을 30%에서 40%로
  변경했다.
- 안전 높이 수직 상승과 P1 상공 최종 하강 속도는 20%로 유지했다.

## 2026-08-14 — PCB 티칭 포인트 기반 통합 Pick→Place 상공 워크플로

- WebApp 티칭 포인트 `PCB`를 조회했다:
  TCP/Base `[226.070,-145.954,104.784,179.997,0.005,180.000]`, Tool 1/User 0.
- `ClosePin`은 부품 관측 위치
  `[-215.278,-20.761,104.748,-180.000,0.000,89.999]`로 사용한다.
- 한 명령으로 `ClosePin 이동 → 부품 검출 → 접근/105 mm 하강/파지/50 mm
  상승 → 부품을 든 채 PCB 이동 → P1 30프레임 저장 → 최신 P1 표면 100 mm 상공 운반`을
  수행하는 `run_full_pick_to_board_hover.sh`를 추가했다.
- 수평 운반 40%, 기판 상공 수직 이동 20%, 마지막 5 mm 하강 15%이며 기판
  표면 하강과 그리퍼 개방은 포함하지 않는다.
- 파지 전 실패는 안전 복귀할 수 있고, 파지 후 실패는 자동 복귀하지 않고
  정지 유지한다.
- 첫 실행은 `robot_mode=1`(수동) 안전검사에서 PCB 이동 명령 전에 중단됐다.
  실제 로봇 이동은 발생하지 않았다. AUTO 모드 전환 후 재실행해야 한다.

#### 통합 워크플로 파지 확인 강화

- 통합 실행에서 그리퍼가 부품을 잡지 않은 채 소폭 상승하고 종료되는 증상이
  확인됐다.
- 닫힘 명령 후 최신 `grip_motion_done`을 기다리고
  `GetGripperCurPosition(1)`로 실제 위치를 다시 읽도록 수정했다.
- 닫힘 명령 후 실제 위치가 95 이상으로 계속 열려 있으면 상승을 차단한다.
- 파지 완료 후 0.5초 안정화 시간을 둔 다음에만 50 mm 상승한다.

#### ClosePin/PCB 티칭 포인트 이동 속도 분리

- 티칭 포인트 이동의 수직 속도가 기판 상공 접근 속도 20%를 공유해 느리던
  구조를 분리했다.
- 사용자 요청에 따라 `ClosePin`과 `PCB` 티칭 포인트 이동은 수평 50%, 수직
  50%를 기본으로 올렸다.
- 부품 안전 접근과 기판 목표점 100 mm 상공 이동도 50%를 기본으로 한다.
- 부품 파지 직전 마지막 5 mm 하강만 안전상 기존 15%를 유지한다.

#### 그리퍼 자세 회전 속도 통일

- 통합 Pick→PCB 상공 워크플로와 개별 부품 접근 코드의 그리퍼 자세 회전
  속도를 50%로 통일했다.
- `ClosePin`/`PCB` 티칭 포인트 자세 전환과 기판 상공 운반의 기본 수평·회전
  이동도 50%를 사용한다.
- 충돌 위험이 큰 부품 직전 마지막 5 mm 수직 하강은 회전과 무관하므로 15%를
  유지한다.

#### 파지 후 PCB 이동 거리 제한 수정

- 그리퍼 위치 5로 정상 파지했고 TCP가 파지점에서 50 mm 상승한 것을 확인했다.
- 이후 멈춘 원인은 `ClosePin` 파지 위치에서 `PCB` 티칭 포인트까지 거리가 약
  550 mm인데, 티칭 이동 안전 제한이 500 mm였기 때문이다.
- 통합 워크플로의 티칭 포인트 이동 제한을 800 mm로 확장했다.
- 이동 경로는 기존처럼 먼저 안전 Z로 수직 상승한 뒤 수평 이동하고 PCB 높이로
  하강하므로 장거리 대각선 이동은 하지 않는다.

#### 기판 가로 방향 배치 자세 적용

- 기존 기판 상공 이동은 파지 자세를 그대로 유지해 부품이 세로 방향으로
  도착하는 문제가 있었다.
- 저장된 기판 pose의 X축을 기판 가로 방향으로 사용하고, 파지에 사용한 Tool Y
  축(부품 장축)을 해당 방향에 맞추도록 목표 TCP 자세를 계산한다.
- 안전 Z에서 50% 속도로 자세를 먼저 회전한 뒤 배치 슬롯의 100 mm 상공으로
  이동한다. 기판 표면 하강과 그리퍼 개방은 아직 수행하지 않는다.

#### PCB 목표 수집 안정화 및 속도 정책 정리

- `right_white_brown_02`가 정상 선택됐지만 PCB pose 30프레임의 최대 흔들림이
  14.552 mm라서 안전 차단되어 PCB 티칭 위치에서 멈췄다.
- 구독 큐를 1로 줄이고 시작 10프레임을 버린 뒤, 최근 30프레임 전체가 1 mm
  이내로 안정될 때만 목표 파일을 저장하도록 변경했다.
- 일반 수평·수직 이동과 모든 자세 회전은 50%로 통일했다.
- 저속 15%는 파지 직전 마지막 5 mm 하강과 파지 직후 최초 5 mm 상승에만
  적용하고, 이후 나머지 상승과 PCB 운반은 50%로 수행한다.
- PCB pose 불안정으로 파지 후 중단된 경우 Pick을 반복하지 않고 현재 PCB
  위치에서 목표를 재검출해 100 mm 상공까지 이어가는
  `run_resume_pcb_to_board_hover.sh`를 추가했다.

#### P1/P2 목표 토픽 충돌 제거

- 상시 실행 중인 기본 P1 `board_view`와 임시 P2 계산 노드가 동일한 target/image
  토픽을 발행해 P1/P2 표시와 좌표가 교대로 바뀌는 현상을 확인했다. 약 14.6 mm
  흔들림은 두 슬롯 좌표가 번갈아 수집된 결과였다.
- 기본 화면은 목표를 선택하지 않아 모든 슬롯을 하늘색으로 표시하도록 변경했다.
- 명령 실행용 노드는 별도 이름 `board_target_capture`와 전용 토픽
  `/vision/board/capture/target_pose`, `/vision/board/capture/image/compressed`를
  사용해 기존 화면 및 좌표와 충돌하지 않도록 분리했다.

#### 슬롯별 부품 배치 방향 및 기본 화면 타겟 연동

- 실제 배치 규칙을 P1 가로(기판 X축 0°), P2~P5 세로(기판 Y축 90°)로
  `physical_board.json`에 저장했다.
- 상공 이동 코드는 선택된 슬롯 ID로 각도를 읽으며, 모든 슬롯에 0°를
  하드코딩하던 옵션을 제거했다.
- 통합/복구 명령이 `/vision/board/selected_target`을 발행해 기본 기판 화면에서도
  실행 중인 목표만 분홍색으로 표시하고, 명령 종료 후 다시 모두 하늘색으로
  돌아오도록 연결했다.

#### 파지 후 중복 상승 제거 및 이동 속도 통일

- 기존 `파지 후 5 mm 저속 상승 → 총 50 mm까지 추가 상승 → PCB 안전 경로
  상승`의 중복 동작을 제거했다.
- 파지 후에는 5 mm만 15%로 상승하고, 이후 PCB 티칭 이동의 안전 Z 상승부터
  모든 수평·수직·회전 이동을 50%로 수행한다.
- 파지 직전 마지막 5 mm 하강과 파지 직후 5 mm 상승만 15%이며 나머지는
  모두 50%다.
- `ClosePin` 및 `PCB` 티칭 이동 전에 `SetSpeed(50)`을 명시해 WebApp의 낮은
  전역 속도 설정 때문에 MoveCart가 느려지는 현상을 방지했다.

#### 세로 부품 반복 배치 오프셋 보정

- P3에 동일 부품을 두 번 배치한 결과 목표 대비 각각 약 `[+1.0,+4.2] mm`,
  `[+1.2,+4.1] mm`로 같은 방향의 오차가 반복됐다.
- 평균 실제 배치 오차 `[+1.1,+4.15] mm`의 반대값을 TCP 명령에 적용하도록
  P2~P5 세로 슬롯에 기판 좌표 보정 `[-1.10,-4.15] mm`를 저장했다.
- 슬롯 중심 좌표 자체는 흰 종이로 등록한 실제 위치이므로 변경하지 않았다.
- P1은 가로 파지 방향이라 동일 보정을 사용하지 않고 별도 검증 대상으로 남겼다.
- 1차 보정 적용 후 P3 잔차는 약 `[+0.30,-0.10] mm`로 감소했다. 반대 잔차를
  추가해 P2~P5 최종 후보 보정을 `[-1.40,-4.05] mm`로 미세 조정했다.
- 필요하면 `--teaching-vertical-speed-percent`로 티칭 포인트 수직 속도만
  별도 조절할 수 있다.
## 2026-08-17 — D435 ChArUco 기반 부품 파지 방식 정리

- 현재 부품 파지 방식은 YOLO나 일반 카메라 전체 화면 탐색이 아니라, 고정된
  ChArUco 보드를 기준 좌표계로 사용하고 각 검정 셀 내부에서 부품을 찾는
  RGB 기반 방식이다.
- ChArUco 코너들을 검출해 보드 평면을 homography로 정면 보정한 뒤, 모든 검정
  셀을 순회한다. 셀 내부의 밝기 threshold와 `light/orange/brown/any` HSV
  profile을 결합하고, 예상 부품 면적·윤곽 크기 조건으로 후보를 고른다.
- 후보 중심은 보드 좌표로 계산하고, ChArUco 보드 pose와 저장된 Eye-in-Hand
  `T_flange_camera`, 현재 FR5 flange pose를 연결해 Base 좌표로 변환한다.
- 부품의 긴 변은 `minAreaRect`로 구하고 Base XY 각도로 변환한다. 선택 옵션에서
  그리퍼 `tool_x` 또는 `tool_y` 축을 긴 변에 맞춰 RZ를 보정한다.
- 20~30개 안정 프레임의 중앙값을 사용하며, 현재 실행 시 기본 30프레임과 Base
  jitter 제한으로 최종 target JSON을 만든다.
- 높이는 당시 RGB 보드 평면과 입력한 부품 높이(`6 x 3.5 x 2.5 mm`)로 계산한
  등록값을 사용했다. Depth 토픽도 구독하지만 동기화·유효값이 없으면 RGB 등록
  높이로 진행하므로, 지금까지의 파지는 Depth 단독 측정 방식이 아니다.
- 이후 `move_object_approach.py`가 target에서 100 mm 위로 접근하고, 별도
  수직 하강 시험에서 100 mm + 마지막 5 mm를 내려간 뒤, `MoveGripper(1,5)`로
  닫고 상승한다. TCP는 toolcoord1에서 처리하며 Hand-Eye에 중복 가산하지 않는다.
