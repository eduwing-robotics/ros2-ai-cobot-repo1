# 2026-09-03 작업일지

## 시작 기준과 안전 판단

- 2026-09-02 종료 일지를 확인했다.
- 어제 종료 시 CAP-01을 파지한 채 하강 중 정지했고 `abnormal_stop=1`, `robot_motion_done=0`이었으므로 새 로그인에서 로봇 상태를 다시 읽기 전에는 어떤 이동·그리퍼 명령도 보내지 않았다.
- 오늘 통합 스택 기동 후 읽은 상태는 `robot_mode=1`(수동), `robot_motion_done=1`, Tool1/User0, 비상정지·이상정지·주/부 오류·충돌 오류 0이다.
- 현재 TCP/Base는 약 `[85.745, -558.521, 276.608, -179.999, -0.027, 0.750]`이다.
- 그리퍼 피드백은 유효하고 위치 값은 0이지만, 어제 CAP-01 파지 기록과 현재 실물 상태가 일치하는지는 작업자가 직접 확인해야 한다. 확인 전 자동 이동은 계속 금지한다.
- 오늘은 서버·카메라·비전 프로세스만 기동했으며 로봇 이동이나 그리퍼 명령은 보내지 않았다.

## 어제 미완료 보정 반영

- GPU 최종 TCP Z를 `88.16085 mm`에서 `88.46085 mm`로 `+0.3 mm` 올렸다.
- HBM 표면 상대 놓기 오프셋을 `-0.26208 mm`에서 `+0.03792 mm`로 `+0.3 mm` 올렸다.
- 두 값은 어제 실제 조립에서 부품을 누르는 경향을 확인한 작업자 판정을 반영한 재시험 후보값이다. 새 스냅샷과 저속 실기 재검증은 아직 필요하다.

## 트레이 일괄 스냅샷 품질 게이트

- 트레이 분할 검출에 마스크 면적 일치도, 종횡비 일치도, 직사각형성 및 종합 마스크 형상 점수를 추가했다.
- 안정화 트랙 결과에 검출 신뢰도, 마스크 형상 점수, 직사각형성의 중앙값을 저장하도록 변경했다.
- 부품별 최소 검출 신뢰도와 형상 기준을 레시피에 추가했다.
- 일괄 스냅샷이 필수 품질 지표가 없거나 부품별 하한을 통과하지 못하면 계획 생성을 거부하도록 변경했다.
- 어제처럼 낮은 신뢰도의 PM 검출이 프레임 수와 수량만으로 통과하는 경로를 차단했다.

## SMD/CAP 2세트 중 1세트(5개) 근접뷰 준비

- SMD 트레이의 물리 요구 개수는 10개(2세트×5개)로 유지한다.
- 현재 조립 사이클은 `--set-index 1|2`로 한 줄 5개만 선택한다. 다른 줄이 비어 있거나 채워져 있어도 선택한 세트만 사용한다.
- 근접뷰 캡처가 `--all-instances`로 선택한 CAP 5개를 동시에 안정화하고 인스턴스별 각도·신뢰도를 한 파일에 저장할 수 있도록 확장했다.
- 기본 출력은 `smd_close_targets_current.json`, 결과 스키마는 다중 프레임·다중 인스턴스 형식으로 갱신했다.
- 고정 사이클 스냅샷에 SMD 근접뷰 입력을 읽는 검증 도우미를 추가했다.
- 다음 단계는 CAP 5개 근접뷰 각도를 실제 촬영하고 TrayHome XYZ와 필수 병합하는 경로를 완성하는 것이다. 유효하지 않은 근접뷰를 전체뷰 각도로 대체해서는 안 된다.

## FR5 조립 스택 통합 실행기

- 프로젝트 루트에 `run_fr5_assembly_stack.sh`를 추가했다.
- 한 명령으로 다음 구성요소를 순서대로 기동한다.
  1. FAIRINO 명령/상태 서버와 Unity ROS-TCP Endpoint
  2. D435 RGB-D 카메라
  3. 트레이 등록·부품 검출·SMD 근접뷰·트레이 렌더러
  4. 기판 화면과 기판 3D 좌표 추적
  5. 트레이/기판 통합 작업자 화면
  6. Unity 관제용 Vision Action API
- `start`, `stop`, `restart`, `clean`, `status`, `check`, `logs`, `follow`, `view` 명령을 제공한다.
- 기본 카메라는 기존 최종 운용 방식과 같은 단일 1280×720 RGB-D `standard` 프로필이다. TrayHome과 SMD 근접뷰가 이 스트림을 함께 사용한다.
- 1920×1080 `smd` 프로필은 진단용 선택지로만 남긴다. 정상 사이클에서 사용하면 1280 기준 TrayHome homography 및 화면 연속성과 어긋난다.
- `--smd-set 1|2`로 전체 10개 중 이번 사이클에서 사용할 5개 세트를 선택한다.
- PID와 로그는 Git에서 제외된 `runtime/assembly_stack/`에 저장한다.

## 중복 노드와 재시작 처리

- 시작 전에 통합 실행기가 관리하던 프로세스를 역순으로 종료하고, 알려진 FAIRINO·Unity Endpoint·D435·트레이·기판·비전 API 잔존 프로세스를 이름으로 찾아 정리한다.
- 로봇 상태를 받을 수 있는데 `robot_motion_done != 1`이면 FAIRINO 서버 정리를 거부한다.
- 종료는 SIGINT 후 SIGTERM까지만 사용하며 SIGKILL은 자동으로 사용하지 않는다.
- 첫 재시작 시험에서 종료 직후 ROS 그래프에 `/fr_command_server`가 잠시 남아 외부 런처가 중복으로 오인하는 현상을 확인했다.
- `/fr_command_server`가 ROS 그래프에서 연속 두 번 사라진 것을 확인한 뒤 새 서버를 시작하도록 수정했다.
- 대용량 이미지 토픽을 직렬화해 준비 여부를 검사하던 방식은 고해상도에서 timeout이 발생해, 토픽의 publisher count를 검사하는 방식으로 바꿨다.
- 기동 중 어느 필수 단계라도 실패하면 이번 시도에서 시작한 모든 구성요소를 자동 롤백한다.

## 실제 기동 검증

- 안전 정지 상태에서 `restart`로 기존 7개 프로세스 그룹이 역순 종료되는 것을 확인했다.
- 수정 후 `start` 전체 기동 성공:
  - `/nonrt_state_data` 준비
  - TCP 10000 포트 준비
  - D435 컬러 및 정렬 depth 준비
  - 트레이 부품 수량, 기판 화면, 기판 3D 포즈, 통합 화면 준비
  - `/vision_orchestration_action_server` 준비
- 핵심 노드 8개가 각각 정확히 한 개만 실행 중인 것을 확인했다.
- 현재 통합 스택은 계속 실행 중이다.
- 잘못 적용했던 1920 프로필을 기존 최종 운용 기준인 단일 1280×720 RGB-D로 복구했다.
- 복구 후 `/vision/tray/registration`은 `TRACKING`, `at_trayhome=true`로 확인했다.
- RQT 통합 영상은 publisher 1, subscriber 1로 연결됐고 핵심 ROS 노드는 각각 1개만 실행 중이다.

## 검증 결과

- `bash -n run_fr5_assembly_stack.sh` 통과.
- 변경한 Python 비전 스크립트 4개 `py_compile` 통과.
- SMD 2세트 선택, 고정 사이클 품질 게이트와 놓기 방향 전체 테스트: `14 passed`.
- 변경 파일 `git diff --check` 통과.

## 다음 작업

1. 작업자가 로봇 주변, 현재 CAP-01 유무, 그리퍼 상태를 실물로 확인한다.
2. CAP-01을 잡고 있다면 작업자 지시에 따라 안전 회수 절차를 먼저 정한다.
3. SMD 근접뷰에서 CAP 5개 각도를 새로 캡처하고 고정 사이클 스냅샷의 필수 입력으로 병합한다.
4. PM은 마스크 품질 게이트를 적용한 새 검출로 픽 중심을 재검증한다.
5. VRM 공통 파지 Z를 기존보다 0.3 mm 높인 새 오프셋 `-7.932 mm`로 다음 실제 사이클 전에 한 개를 저속 재확인한다.
6. GPU/HBM `+0.3 mm` 놓기 Z를 새 기판·트레이 스냅샷과 저속 단일 부품으로 재검증한다.

## VRM 1~5 제자리 파지 시험 및 공통 Z 조정

- 공통 픽 중심 보정은 Base `X -2.0 mm`, `Y +1.6 mm`, 그리퍼는 열림 `28`, 파지 `24`를 사용했다.
- 이동 속도는 수평·회전 `20%`, 수직·최종 하강 `10%`로 유지했다.
- VRM-01은 검출 표면 Z `-45.314 mm`, 기존 최종 파지 Z `-53.546 mm`에서 파지해 총 `55 mm` 상승했고 작업자가 중심과 실제 들림을 확인했다.
- VRM-02는 검출 표면 Z `-46.403 mm`, 기존 최종 파지 Z `-54.635 mm`에서 파지해 `25 mm` 상승했다. 그리퍼 실제 위치 `24`와 상승 완료를 확인한 뒤 제자리에 놓았다.
- VRM-03은 검출 표면 Z `-45.482 mm`, 기존 최종 파지 Z `-53.714 mm`에서 파지·`25 mm` 상승·제자리 복귀 사이클을 완료했다.
- VRM-04는 검출 표면 Z `-47.546 mm`, 기존 최종 파지 Z `-55.778 mm`에서 파지·`25 mm` 상승·제자리 복귀 사이클을 완료했다.
- VRM-05는 검출 표면 Z `-44.675 mm`, 기존 최종 파지 Z `-52.907 mm`에서 파지·`25 mm` 상승·제자리 복귀 사이클을 완료했다.
- VRM-03~05는 그리퍼 닫기 완료 상태와 상승 경로 완료를 확인했으며, 번호별 정지 후 작업자 육안 중심 확인은 생략했다.
- 작업자 판단으로 최종 파지 깊이가 여유 없이 맞아 눌림을 피하도록 VRM 공통 `tcp_z_offset_from_detected_surface_mm`를 `-8.232 mm`에서 `-7.932 mm`로 변경했다. 이후 모든 VRM은 기존보다 `0.3 mm` 높은 위치에서 그리퍼를 닫는다.
- 모든 시험 부품은 원래 자리에 다시 놓았고 로봇은 TrayHome으로 복귀했다.

## Inductor-01 수동 중심 보정

- TrayHome 검출 중심은 Base `[-619.911, -226.888] mm`, 기존 공통 보정 적용 목표는 `[-620.911, -222.888] mm`였다.
- 파지 예정점 5 mm 위에서 작업자가 수동으로 중앙을 맞춘 TCP는 Base `[-620.414, -221.375, -50.100] mm`, C `0.193 deg`였다.
- 수동 이동량은 기존 목표 대비 Base `X +0.497 mm`, `Y +1.513 mm`이며 Z와 각도는 유지됐다.
- 작업자가 중앙임을 확인한 값을 반영해 인덕터 공통 중심 보정을 Base `[-1.0,+4.0] mm`에서 `[-0.503,+5.513] mm`로 갱신했다.
- 아직 최종 5 mm 하강 및 파지·상승은 수행하지 않았다.
- Inductor-02에서 작업자가 다시 맞춘 중심은 Base `[-619.202,-198.855] mm`였고, 해당 검출 중심 대비 보정은 `[-0.006,+5.516] mm`였다.
- 1번과 2번의 보정 차이는 주로 Base X `0.497 mm`였으므로 작업자 지시에 따라 두 값의 중간인 Base `[-0.254,+5.515] mm`를 새 공통값으로 적용했다.
- 인덕터는 트레이 진입 여유를 위해 `tray_pick_open=21`을 별도로 추가했다. 파지 닫힘은 `14`, 보드 배치 해제는 기존 `release=20`을 유지한다.

## SMD TrayHome 표시 깜빡임 진단

- 10초 샘플에서 SMD 원검출 수가 `3→4→5`로 변했으며 다른 부품 수는 고정됐다.
- SMD 마스크는 영상에서 약 `9×12 px`이고, 왼쪽에서 오른쪽으로 모델 신뢰도가 대략 `0.665, 0.410, 0.29, 0.28, 0.31`로 낮아졌다.
- 통합 트레이 실행기가 검출기 기본값 대신 `overlay-hold-frames=1`, `count-smoothing-frames=3`을 강제해 단일 누락도 RQT에서 즉시 깜빡이게 했다.
- 검출 임계값과 조립 품질 게이트는 유지하고 표시 안정화만 기본값인 오버레이 `5프레임`, 개수 평활 `7프레임`으로 복구했다.

## 주요 파일

- `run_fr5_assembly_stack.sh`
- `docs/FR5_ASSEMBLY_STACK_COMMANDS.md`
- `vision_assembly/config/part_gripper_recipes.json`
- `vision_assembly/config/smd_section_view.json`
- `vision_assembly/scripts/detect_tray_parts.py`
- `vision_assembly/scripts/fixed_cycle_snapshot.py`
- `vision_assembly/scripts/capture_smd_close_target.py`
- `vision_assembly/tests/test_fixed_cycle_snapshot.py`

## SMD-01 근접각·파지 방향·절대 Z 재검증

- 최초 근접 캡처의 Base 장축 `89.995 deg`는 TrayHome 장축 `1.647 deg`와 modulo-180 기준 `88.348 deg` 모순이었다. 근접 OBB가 직각 축을 선택한 오검출인데 당시 병합 로직이 90도 차이를 허용해 차단하지 못했다.
- 잘못된 근접각을 적용한 뒤 `tool_x`에서 `tool_y`로 바꿔 겉보기 방향만 보상한 상태로 하강해 그리퍼와 부품이 접촉했다. 접촉 전후 TrayHome 중심 비교에서 부품은 XY 약 `1.753 mm`, Z 약 `+1.040 mm` 이동했다.
- 따라서 충돌 원인은 미세각 미적용이 아니라 **90도 잘못 검출된 미세각의 적용, 잘못된 그리퍼 축 임시 변경, 올바른 180도 손목 분기 미선택**의 결합이다.
- 재캡처한 근접 장축은 Base `177.212 deg`이고 TrayHome 장축과의 모순 오차는 `4.435 deg`로 정상이다. 앞으로 coarse/close 오차가 `10 deg`를 넘으면 병합 및 실행을 차단한다.
- SMD 파지축은 원래 검증값인 `tool_x`로 유지한다. SMDSectionView의 TCP C 약 `90 deg`에서 음수 대칭 분기를 선택해 최종 TCP C 약 `0 deg`, J6 증가 쪽 해를 사용한다.
- 실제 재검증 최종 자세는 TCP C `-2.788 deg`, J6 `+98.675 deg`였으며 작업자가 양쪽 흰색 끝단 파지 방향을 확인했다.
- TrayHome 검출 Z에 상대 오프셋을 적용한 `-51.34 mm` 부근에서는 닫힘 `12` 후 20 mm 상승해도 부품이 들리지 않았다. 작은 SMD의 TrayHome depth 변동을 최종 파지 Z에 사용하면 안 된다.
- 작업자 지시에 따라 SMD 픽 Z는 수동 티칭한 고정 절대값 `-53.153 mm`를 사용한다. 기존 `-5.862 mm` 상대 오프셋은 진단 기록으로만 남기고 실제 최종 Z 계산에는 사용하지 않는다.
- SMD 그리퍼 값은 트레이 픽 진입 열림 `18`, 파지 닫힘 `12`, 기판 조립 해제 `17`로 분리 저장했다. `+0.3 mm` 파지 높이 조정은 VRM 1~5에만 적용하며 SMD에는 확대 적용하지 않는다.
- `move_object_approach.py`에 180도 대칭 회전의 `shortest/positive/negative` 분기 선택을 추가하고 SMD 안전 픽 실행기가 레시피의 음수 분기, 최대 회전 `95 deg`, 최대 관절 스텝 `96 deg`를 전달하도록 연결했다.
- `safe_part_pick.py`는 SMD의 `fixed_fixture_absolute` Z 모드를 읽어 검출 surface Z와 무관하게 `-53.153 mm`를 사용한다.
- 회귀 테스트는 총 `20 passed`이다.

## 현재 SMD 실기 상태

- TCP는 SMD-01 파지 높이 약 `[-621.144, -164.045, -53.160, 179.998, -0.002, -2.788]`에서 정지했다.
- 그리퍼는 열림 `17`이며, 새 트레이 진입 열림값 `18`은 다음 픽 준비부터 적용한다.
- 충돌·주요 오류·그리퍼 오류는 없다.
- 새 절대 Z에서 그리퍼 `12` 재닫기와 수직 상승 파지 확인은 아직 수행하지 않았다.

## SMD-02 각도 부호 오검출과 실행 차단 강화

- 통합 RQT용 카메라 프로파일은 의도한 `1280x720x15`가 맞으며 변경하지 않았다.
- SMD-02 TrayHome 장축은 Base `1.657 deg`였고, 근접 캡처 16/24/24프레임은 모두 내부 각도 안정성 검증에 실패했다.
- 수동 시험 중 실패한 두 24프레임 묶음의 중앙값만 합의해 `+7.026 deg`를 사용한 것은 잘못된 우회였다. 작업자가 파지 높이에서 부품은 왼쪽 아래, 그리퍼는 오른쪽 아래로 기울어 부호가 반대임을 확인했다.
- 작업자 확인에 따라 시험 방향을 `-7.026 deg`로 바꿨고 파지 절대 Z `-53.152 mm`에서 방향을 확인했다. 그리퍼는 닫지 않았으며 이후 수직으로 50 mm 상승했다.
- `merge_smd_coarse_and_angle.py`의 불안정 다중묶음 우회는 완전히 제거했다. 단일 근접 캡처와 해당 부품이 모두 `validation_passed=true`여야 병합된다.
- 단일 병합기와 25개 고정 사이클의 coarse/close 각도 기본 허용차를 `10 deg`에서 `5 deg`로 강화했다. 실제 실패 조합 `1.657 deg / +7.026 deg`가 예외로 차단되는 회귀 검증도 추가했다.
- 회귀 테스트는 `20 passed`, 관련 변경 파일 `git diff --check`를 통과했다.
- 현재 TCP는 약 `[-621.059, -151.339, -3.155, 179.999, -0.002, -7.026]`, 그리퍼는 열린 값 `18`로 정지 상태다.

## Real gripper feedback requirement audit and ROS-RBT-009 Real Ghost

- The interrupted SMD return command did not start. The robot remained stationary at the SMD-05 100 mm hover, TCP approximately `[-621.429, -116.318, 53.471, 179.998, -0.002, 159.249]`, gripper 18, with no collision or controller errors.
- The v3.9.7 source already appends `gripper_position` and `gripper_feedback_valid` to the ROS publisher, and the live topic carries both fields. A read-only cross-check returned SDK `GetGripperCurPosition(1)=0,0,18`, topic position `18`, and `gripper_feedback_valid=true`.
- `/nonrt_state_data` remained at approximately 100 Hz (measured 99.966 to 100.007 Hz).
- The supplied gripper requirement is not fully met yet: the two message fields are currently located after `grip_motion_done`, not at the end of `RobotNonrtState.msg`; and the valid expression checks only `position <= 100`, not `GetRobotRealTimeState success AND gripper_active AND range`. Unity source files referenced by the requirement were not present on this PC, so its regenerated message class and exception status could not be verified locally.
- Added `fr5_process_sequences.real_ghost.RealGhostTargetPublisher` for ROS-RBT-009. It creates and publishes only in the `real` backend, uses `/real/ghost/target` with `geometry_msgs/msg/PoseStamped`, requires `base_link`, preserves Tool 1 TCP metres without applying Unity's TCP offset, and normalizes the quaternion.
- The Ghost path is fire-and-forget: it does not wait for a subscriber or response, ignores non-motion process steps, and returns false instead of raising on validation or transport failure.
- The repository currently contains a dry-run process planner but no commissioned `real_assembly` execution node. The publisher is therefore exposed as a backend integration API at the exact confirmed-target call site rather than being attached to a nonexistent executor.
- Added package dependencies, API documentation, and six Ghost contract tests. `colcon build --packages-select fr5_process_sequences --symlink-install` succeeded; package tests reported `15 passed`.

## Real gripper feedback 활성 조건 반영 및 재배포

- `fr_command_server`의 `gripper_feedback_valid`를 `GetRobotRealTimeState()` 성공 블록 안에서 `gripper_active != 0 AND gripper_position <= 100`으로 계산하도록 수정했다. SDK 조회 실패 시에는 기본값 `false`가 발행된다.
- `RobotNonrtState.msg`의 기존 필드 순서는 현재 ROS와 Unity 생성 메시지의 직렬화 호환성을 위해 변경하지 않았다.
- `fairino_msgs`, `fairino_hardware_v3_9_7`, 설명 및 MoveIt 설정을 포함한 `robot_ws/build_robot_ws.sh` 빌드가 성공했다. 벤더 소스의 기존 경고는 있었지만 빌드 오류는 없었다.
- 재시작 전 TCP `[-621.435, -116.315, 53.475, 179.998, -0.002, 159.249]`, `robot_motion_done=1`, 비상·충돌·주요 오류 0을 확인했다.
- 통합 실행기로 `--camera standard --smd-set 1` 설정을 유지해 제어 서버, ROS-TCP Endpoint 및 비전 스택을 재시작했다. 로봇 이동 및 그리퍼 동작 명령은 보내지 않았다.
- 재시작 후 직접 조회는 활성 상태 `0,0,1`, 그리퍼 위치 `0,0,18`이었다. 토픽도 `gripper_position=18`, `gripper_feedback_valid=true`, 그리퍼·제어·충돌 오류 0으로 일치했다.
- 안정화 후 `/nonrt_state_data` 발행률은 500 샘플 창 기준 `99.998~100.019 Hz`였다.
- Unity 전달 문서와 ZIP을 새 활성 조건 설명으로 갱신했다. Unity 실제 프로젝트의 메시지 재생성, 예외 제거, UI/모델 반영 검증은 Unity 쪽에서 수행해야 한다.
- Live ROS verification from a test node named `real_assembly` received `frame_id=base_link`, position `[0.4, 0.0, 0.3] m`, and quaternion `[0,0,0,1]` on `/real/ghost/target`. The temporary publisher exited after the check.


## SMD pilot_06 축 검출 및 공통 파지값 최종 확인

- 휴대폰 촬영·라벨 데이터를 사용한 SMD OBB 학습 결과를 검토하고 `pilot_06` 모델을 근접뷰 기본 모델로 적용했다.
- 부품의 단순 사각형 장축이 아니라 양쪽 흰색 단자를 잇는 축을 파지축으로 사용한다. SMD 그리퍼 축은 `tool_x`이며 두 손가락이 흰색 양끝단을 잡도록 한다.
- 24프레임을 3개 시간 배치로 나눠 각 배치 중앙 축, 축 비율, 배치 간 각도 편차와 로봇 자세 안정성을 모두 통과해야 각도를 채택하도록 변경했다.
- SMD-04에서 작업자가 맞춘 공통 중심 보정은 Base `X -1.589 mm`, `Y +3.779 mm`, 공통 절대 파지 TCP Z는 `-52.177 mm`다.
- 트레이 픽 열림 `18`, 파지 닫힘 `12`, 보드 조립 해제 `17`, 이동·회전 `20%`, 수직·최종 하강 `10%`를 저장했다.
- SMD-04와 SMD-02에서 새 공통 중심·절대 Z·`tool_x` 방향을 실제 파지 깊이까지 재확인했다.
- `capture_smd_close_target.py`의 기록용 고정 보정값도 이전 `[+0.041,+4.548,0]`에서 새 `[-1.589,+3.779,0]`으로 일치시켰다.
- 관련 레시피·단자축 회귀 테스트는 `15 passed`, `git diff --check`를 통과했다.

## 오늘 확정된 트레이 픽 레시피

- Power Module: 공통 중심 보정 Base `[-2.309,+0.665] mm`, 파지 닫힘 `25`, 해제 `30`. PM-01과 PM-02에서 실제 들림을 확인했다.
- VRM: 공통 중심 보정 Base `[-2.0,+1.6] mm`, 트레이 열림 `28`, 파지 `24`, 해제 `26`. 눌림 방지를 위해 공통 표면 상대 파지 오프셋을 `-7.932 mm`로 0.3 mm 높였다.
- Inductor: 두 부품 중심의 중간값 Base `[-0.254,+5.515] mm`, 트레이 열림 `21`, 파지 `14`, 조립 해제 `20`을 저장했다.
- SMD: Base `[-1.589,+3.779] mm`, 고정 TCP Z `-52.177 mm`, 트레이 열림 `18`, 파지 `12`, 조립 해제 `17`을 저장했다.
- GPU 최종 놓기 TCP Z `88.46085 mm`, HBM 표면 상대 놓기 오프셋 `+0.03792 mm`로 각각 기존보다 0.3 mm 높인 값이 유지돼 있다.
- SMD 작업 중 GPU·HBM·Power Module·VRM·Inductor의 다른 저장값은 추가로 변경하지 않았다.

## 25개 전체 사이클용 새 측정

- 보드 `PlaceCamera`에서 새 3D 보드 자세를 캡처했고 25/25 놓기 위치가 모두 준비됐다.
- TrayHome에서는 HBM 1세트 8개 외에 두 번째 세트 쪽 5개가 같이 보여 총 30개가 검출됐다. 레이아웃의 `per_set_count=8`과 공간 군집을 근거로 첫 HBM 8개만 선택하고 나머지 5개는 제외했다.
- PM-02의 YOLO 소프트 점수는 `0.136`으로 전역 하한 0.70보다 낮았다. 다만 40프레임 안정, 마스크 형상 `0.910`, 사각도 `0.946`, 당일 실제 픽 검증을 근거로 저장 레시피는 바꾸지 않고 이번 사이클 임시 레시피에서만 하한 `0.10` 예외를 기록했다.
- SMD 근접뷰에서 5개를 각각 24프레임 촬영했고 모두 단자축 검증을 통과했다. 새 근접 각도를 TrayHome XYZ에 병합했다.
- `fixed_cycle_full_2026-09-03.json`은 보드·트레이·SMD 근접뷰가 모두 캡처됐고 `ready_for_continuous_execution=true`, 25/25 상태였다.

## 25개 전체 조립 시도 결과

- 실행 순서는 GPU-01, HBM-01~08, PM-01~04, VRM-01~05, IND-01~02, CAP-01~05로 계획했다.
- SMD 계획은 검출 surface Z가 아니라 새 고정 절대 Z `-52.177 mm`를 사용하도록 보정했다.
- 최초 250경유점 검사에서 HBM-04의 180도 대칭 분기가 J6을 `177.544 deg` 뒤집는 해로 선택돼 실행 전에 차단됐다.
- 이를 해결하려고 픽 전과 놓기 전 회전을 안전 Z에서 별도 단계로 분리했고, 분리된 300경유점 계산은 최소 관절 한계 여유 `37.814 deg`, 최대 관절 변화 `93.911 deg`로 통과했다.
- 실제 실행에서 GPU-01과 HBM-01, HBM-02, HBM-03은 픽·운반·놓기·해제까지 완료됐다. GPU는 새 `+0.3 mm` 최종 Z로 놓았다.
- HBM-04는 잡기 전 안전 Z의 수평 트레이 이동 중 컨트롤러가 정지했다. 따라서 HBM-04와 이후 부품은 움직이지 않았고 그리퍼에는 부품이 없다.

## 마지막 경로 설계 실수와 오류 149

- HBM-04 트레이 이동 목표 관절은 J6 약 `-188.049 deg`였다. 실제 이동 중 J6 `-182.963 deg` 부근에서 `abnormal_stop=1`, `main_error_code=1`, `sub_error_code=149`, `cmdpointerror=149`, `dr_err=6`이 발생했다.
- 로컬 SDK 정의에서 `dr_err=6`은 PTP 관절 명령 초과다. 사전검사는 컨트롤러가 보고한 J6 소프트 범위 `±360 deg`와 단계 변화 95 deg만 검사했기 때문에 실기 운용 한계를 잡지 못했다.
- 작업자 피드백대로 오늘 만든 `회전 → 수평 이동` 분리 경로는 어제 검증된 실행보다 동작이 끊기고 스무스하지 않았다.
- **오늘의 실수:** 어제 실제로 스무스하게 수행된 전체 실행 구조를 그대로 참고해야 했는데 경로를 과도하게 분리해 동작 품질을 악화했고, 계산상 소프트 한계와 실기 컨트롤러 한계의 차이도 놓쳤다.
- 오류 발생 즉시 실행기는 종료됐고 추가 하강·파지 명령은 보내지 않았다. 당시 충돌 오류는 0, TCP Z는 약 `350.17 mm`, 그리퍼는 열린 `25`였다.
- `ResetAllError()`는 안전 상공·빈 그리퍼·충돌 없음 확인 후 실행했고 성공했다. 해제 후 주/부 오류, 비정상 정지, 명령점 오류, 충돌 오류는 모두 0이다.

## 오늘 종료 시 실제 상태

- 완료되어 보드에 놓인 부품: `GPU-01`, `HBM-01`, `HBM-02`, `HBM-03`.
- 트레이에 그대로 남은 부품: `HBM-04~08`, `PM-01~04`, `VRM-01~05`, `IND-01~02`, `CAP-01~05`.
- 로봇 TCP/Base: 약 `[-554.549, -69.469, 350.172, -179.998, 0.000, -90.167]`.
- 관절: 약 `[-3.131, -82.434, 77.714, -85.279, -90.000, -182.964] deg`.
- 그리퍼 위치 `25`, 피드백 유효, 빈 그리퍼이며 `robot_mode=0`, `robot_motion_done=1`, Tool1/User0이다.
- 비상정지·비정상정지·주/부 오류·충돌·명령점 오류는 모두 0이다.
- `full_25_run_2026-09-03.json`은 `stopped_on_error`이며 완료 슬롯 4개와 오류 149를 보존한다.
- 오류 해제 후 로봇 이동이나 그리퍼 명령을 추가로 보내지 않았다.

## 내일 재개 지점과 금지사항

1. 시작 즉시 `/nonrt_state_data`와 실물을 대조해 위 TCP, 빈 그리퍼, HBM-04 미파지, 보드의 완료 4개를 확인한다.
2. 오늘 만든 분리형 실행 경로를 그대로 재실행하지 않는다.
3. 2026-09-02의 실제로 스무스했던 25개 실행기와 `full_restart_retry2` 계획·실행 기록을 기준으로 경로 구조를 복원한다.
4. 오늘 확정한 트레이 중심/Z/그리퍼 값, GPU·HBM +0.3 mm, SMD pilot_06 근접 각도만 어제 실행 구조에 이식한다.
5. 완료된 GPU-01과 HBM-01~03은 다시 집거나 놓지 않고 HBM-04부터 이어간다.
6. 실제 컨트롤러에서 문제를 일으킨 J6 `-188 deg` 분기를 사용하지 않는다. HBM-04는 반대 180도 대칭 해를 사용하고 실기 J6 범위를 별도 게이트로 검증한다.
7. 좌표 IK 통과만 실행 성공으로 간주하지 않는다. 어제 성공 경로의 중간 자세·관절 분기·명령 형식을 직접 비교한다.
8. 보드나 트레이가 밤사이 움직였으면 기존 스냅샷을 폐기하고 재측정한다. 움직이지 않았더라도 시작 전 고정 상태와 완료 부품 점유를 확인한다.
9. 재개 시 HBM-04 상공 이동 경로를 먼저 검증하고, 문제가 없으면 HBM-04부터 CAP-05까지 어제 방식의 한 번의 스무스한 연속 실행으로 진행한다.

## 오늘 추가 생성된 핵심 산출물

- `vision_assembly/data/fixed_cycle_full_2026-09-03.json`
- `vision_assembly/data/smd_close_targets_full_2026-09-03.json`
- `vision_assembly/data/full_25_plan_2026-09-03.json`
- `vision_assembly/data/full_25_run_2026-09-03.json`
- `vision_assembly/data/smd_2_auto_common_recheck.json`
- `vision_assembly/config/part_gripper_recipes.json`
- `vision_assembly/scripts/capture_smd_close_target.py`
