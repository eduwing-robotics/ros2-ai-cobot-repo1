# 2026-09-06 작업일지

최종 정리 시각: 2026-09-06T21:30:04.232091+09:00

## 당일 최종 정리 — 전체 사이클 성공 및 휴대폰 검사 카메라

### 조립 성공 기준 보존

- 사용자가 전체 25개 부품 한 사이클 성공을 확인했다. GPU1/HBM8/PM4/VRM5/IND2/SMD5.
- 복구·사용자 파지 확인·SMD04/05 수동 단자 지정이 포함된 성공이며 무인 연속 성공이나 정밀 안착 보증은 아니다.
- 기존 기록은 [전체 사이클 성공](../docs/FULL_CYCLE_SUCCESS_2026-09-06.md), [복구 상세](../docs/RECOVERY_2026-09-06.md), [동결 기준](../vision_assembly/checkpoints/full_cycle_success_20260906/README.md)에 보존돼 있다.
- 기존 동결 기준은 1,914개 파일, 실제 슬롯별 실행값·그리퍼 레시피·Tool1·카메라·학습 가중치·수동 SMD 좌표를 포함한다.
- SMD 트레이 픽은 사용자가 좋다고 확인했다. 추가 개선 대상은 기판 배치 오차다.
- CAP01과 CAP02~05는 배치 방향이 다르다. 기존 성공한 방향/높이/로봇 보정값을 휴대폰 화면 보정으로 덮어쓰지 않았다.

### 추가 카메라 검토와 연결

- 기판 상부/비스듬한 고정 카메라: 기판 목표와 배치 후 결과 확인. 아래에서 보는 카메라: 실제 파지 중심/회전 측정. 측면: 하강·그리퍼 해제 때 밀림 확인에 유리하다.
- 휴대폰은 Samsung SM-F936N, Android16, USB ADB serial `R3CTA0K0T9L`.
- 처음 MTP만 확인됐으며 사용자가 USB 디버깅을 허용했다.
- DroidCam `com.dev47apps.obsdroidcam`: 1280×720, 약4초103프레임 수신 확인. 광고 문제로 종료하고 scrcpy로 대체했다.
- scrcpy4.1 공식 Linux x86_64 배포본 사용. SHA256 `ad56ae8bfeedf41e824945c11dbf55fcb092b3e615b9b486f48a50e30d389635` 확인.
- 1080p 시험168프레임 정상 디코딩 후 4K3840×2160/30fps, 후면 camera0, 줌1, 음성OFF로 구성했다.
- 카메라 줌2는 화면 중앙을 확대해 기판 상단이 잘렸으므로, 4K 원본에서 기판 영역을 자르는 방식으로 바꿨다. 이는 광학 2배 줌이나 해상도 향상 보증이 아니다.
- 사용자가 최종 비스듬한 위치에 고정했다. ROI 원본 좌표 `[1040,465,800,450]`, 표시960×540. 고정 후 원본의 기판 폭은 약300픽셀이므로 작은 SMD의 세부 정밀도에 한계가 있다.

### 저지연 영상 전달

- 초기 MKV/FIFO/OpenCV 전달에서 지연을 호소해 중간 컨테이너 단계를 제거했다.
- scrcpy4.1 서버의 H264 패킷을 ADB forward TCP27481로 직접 받고 PyAV18.1.0으로 디코딩한다. 최신 프레임 1개만 표시한다.
- 원본4K/약30fps 유지, PC 패킷 수신부터 표시 요청까지 대체로20~24ms. 촬영·인코딩·USB·디스플레이 지연을 포함한 전체 지연은 측정하지 않았다.
- KST 시각, 배터리 잔량/충전 상태 표시. 배터리5초 갱신,15초 이상 갱신 실패 시 STALE. 영상 수신1초 지연 시 연결 이상 표시.
- 확인 당시 배터리100%, USB 연결, FULL. 이 값은 그 시점의 값이다.

### 기판 슬롯 표시와 위치 보정

- 처음 mm 슬롯 중심/크기로 재구성한 사각형을 투영했으나 실제 홈 위치와 맞지 않았다.
- `assembly_reference_slots_r1.json`의 원본 라벨 다각형을 사용하고 원본 영상 네 구멍과 현재 네 구멍을 직접 대응시키는 방식으로 수정했다.
- 원본 라벨 영상: `/home/juchan-yoon/Desktop/프로젝트 구현 사진/assembly_slot_coordinates_overlay.png`.
- 원본 구멍 픽셀 TL/TR/BR/BL: `[320.5,182.5]`, `[933.5,192.5]`, `[919.5,842.5]`, `[300.5,838.5]`.
- 현재 화면 대응 순서: 우상/우하/좌하/좌상. 현재 기판 방향은 원본 기준과90도 다르다.
- 사용자가 위치는 맞으나 오른쪽이 들려 보인다고 지적했다. 중심을 유지하고 각 슬롯 윤곽만 시계방향1도 회전하는 화면 보정을 추가했다. 정밀 카메라 보정을 대신하는 값이 아니다.
- 사용자가 C03/C04/C05 불일치를 특정했다. 빈 기판 경계를 비교해 세 슬롯만 개별 보정하고 사용자에게 맞는 것 같다는 확인을 받았다.
- 측정 당시 목표 중심(표시픽셀): C03 `[453.5,90.5]`, C04 `[403.25,89.5]`, C05 `[352.5,89.5]`.
- 원본 라벨 좌표에서의 이동량: C03 `[-3.02075195,5.52630615]`, C04 `[-3.93041992,7.97790527]`, C05 `[-4.60659790,11.80633545]`.
- 개별 값은 `slot_display_corrections.json`에 있다. 로봇 기준 좌표·배치각도·그리퍼 레시피는 이 작업에서 변경하지 않았다.

### 표시 안정화와 남은 재발 문제

- Hough 원 검출의 간헐적 튐을 줄이기 위해 밝기70/90/110/130/150에서 구멍 테두리를 검출하고 타원 중심 중앙값을 사용한다.
- 연속60프레임, 중심 흔들림95백분위≤2표시픽셀일 때 기준을 중앙값으로 고정한다.
- 고정 후 목표 중심을 계속 따라 움직이지 않는다. 구멍이 가려지면 숨기고5개 유효 프레임 후 복귀한다.
- 한 구멍 편차>4픽셀 또는 중앙 이동>2.5픽셀이3프레임 지속되면 MOVED에 고정해 표시/측정을 중단한다. R로 재획득한다.
- 8초 검증에서 LOCKED 유지, 표시GPU중심 범위0픽셀, 약30fps를 확인했다. 이는 표시 고정의 결과이며 물리 정확도0오차를 의미하지 않는다.
- **이후 장시간 실행/부품 놓기/재연결 중 MOVED가 여러 번 재발했다. 최종 기록 시점도 MOVED였다. 안정성 문제가 완전히 해결된 것은 아니다.**
- 실제 구멍 이동과 폰/지지대 움직임, OIS/초점/검출 오차를 구분해야 한다. camera0 메타데이터에는 OIS ON, EIS OFF가 관측됐다. OIS가 원인이라고 확정하지 않았다.
- 초기5초 안정화 후 기준 수집을 추가했으나 재발이 남았다. 임계값을 무작정 키우거나 자동으로 MOVED를 무시하지 말 것.

### SMD 실제 검출·검사

- 25개는 목표 슬롯 표시다. **실제 부품 검출/오차 측정은 현재 CAP01~05(SMD5개)에만 구현됐다. GPU/HBM/PM/VRM/IND 검사는 미구현이다.**
- SMD의 밝고 저채도인 외형을 다중 임계값으로 검출하고 크기·ROI·각도 일관성을 검사한다. 모델 재학습은 하지 않았다.
- 실제 윤곽은 청록색, 목표는 기존 슬롯 색이다. 화면오차 dx는오른쪽+, dy는아래쪽+.
- 최소6프레임의 검출 안정성 확인 후 수치를 표시하고 미검출/불확실/기판이동이면 이전 오차값을 남기지 않는다.
- 각도는 긴 축 방향(180도 대칭)이며 부품 극성/앞뒤를 검증하지 않는다. 해상도와 원근 때문에 고정밀 각도값이라고 단정하지 않는다.
- 저장된 조립 영상에서5개 검출, 빈 기판에서5개 미검출, 실제 영상의 알려진3px/2px 이동 시험, 미검출에서mm값이 나오지 않는 검사를 통과했다.
- C03 실물 검증: 놓기→화면 이동→회전→제거를 사용자와 확인했다.
  - 첫 안정 측정: dx−0.43px, dy−1.17px, 각도약+0.32도.
  - 이동: 이전 대비 화면오른쪽+1px, 위쪽−6.5px. 사용자 작업대 방향과 화면 방향은 다를 수 있다.
  - 회전: 시계방향약26도 검출, 짧은 관찰에서STABLE 유지. 외부 각도계 정답은 없다.
  - 제거: C03 NOT_DETECTED, 이전XYZ가 아닌 XY/각도 수치 삭제 확인. 당시 다른4자리도미검출.
- 검증 기록 `c03_live_validation.json`, `c03_translation_validation.json`, `c03_rotation_validation.json`, `c03_removal_validation.json` 보존.

### mm 환산 및 높이 보정 진행 상태

- 화면 `mm*`는 기존 구멍의 board-mm 좌표를 사용하는 **잠정 기판 평면 환산값**이다. board X/Y이며 화면right/down좌표와 다르다.
- 기존 구멍 board-mm값: `[-43.318,-50.033]`, `[50.088,-49.798]`, `[50.019,49.341]`, `[-43.081,49.553]`. 실측 확정값으로 취급하지 않는다.
- SMD높이3.02376mm는CAD후보값이다. `part_surface_height_mm=null`, `height_verified=false`.
- Camera0 센서 K: fx2702.84179688, fy2708.12841797, cx2042.74914551, cy1527.22314453, skew0.
- Android왜곡 k1/k2/k3/p1/p2: `[0.092268,-0.14522044,0.06744335,0,0]`.
- 센서4080×3060→중앙4080×2295→3840×2160→ROI→960×540이라는 **미검증 크롭 가정**으로 K후보를 계산했다.
- 표시K후보 fx3052.6213, fy3058.5921, cx1059.1049, cy734.8638. 최종 영상의 제조사 왜곡 처리 여부도 미확인이다.
- 후보PnP기판점 재투영RMS약0.43px, 카메라기판좌표후보 `[164.71,-189.57,-1049.88]`mm. 이는 모델 적합도일 뿐 실제 카메라 위치/절대정확도 검증이 아니다.
- `metric_geometry.py`에 높이 평면과 카메라 광선 교차 계산을 구현했다. 알려진 합성기하로 검증했다.
- **높이 시차 보정은OFF**, `metric_calibration_valid=false`, `robot_motion_authorized=false`다. 실측구멍간격·SMD윗면높이와 렌즈/크롭/왜곡 검증 전 활성화하지 않는다.
- 사용자에게 구멍 좌우/상하 중심간격과SMD윗면높이 실측을 요청했으나 오늘 확정 답변은 받지 못했다.

### 내일 재개 순서

1. [내일 인계 문서](../docs/PHONE_CAMERA_HANDOFF_2026-09-06.md)를 먼저 읽는다.
2. 폰 USB연결과같은고정위치/렌즈/화면ROI를 확인한다. 마지막 실물 검증에서는C03을제거했으므로기판이빈상태였지만, 내일 실제 상태는 다시 확인한다.
3. 이미 실행중이면 중복 실행하지 않는다. 영상은살아있어도 MOVED면검사가중지된다.
4. 네구멍과폰/지지대가움직이지않는지확인한후 R로기준재획득한다. 반복MOVED의원인을우선분리한다.
5. 구멍간격·SMD높이실측과렌즈/크롭검증을진행한다. mm*를정밀실측으로간주하지않는다.
6. C03 known-distance 이동/회전/제거를추가검증하고C01/02/04/05를각각확인한다.
7. 이후나머지부품검사와배치전후비교로확장한다. 아직휴대폰을통한로봇자동보정은연결하지않았다.

### 조사한 공식 자료

- [OpenPnP Bottom Vision](https://github.com/openpnp/openpnp/wiki/Bottom-Vision): 파지 후 실제 중심/회전 확인.
- [OpenPnP Fiducials](https://github.com/openpnp/openpnp/wiki/Fiducials): 기판 기준점 정합.
- [OpenPnP Camera Settling](https://github.com/openpnp/openpnp/wiki/Camera-Settling): 진동/통신/노출 안정화.
- [OpenCV Homography](https://docs.opencv.org/4.11.0/d9/dab/tutorial_homography.html): 평면 원근 변환.
- [scrcpy 공식 소스](https://github.com/Genymobile/scrcpy), [카메라](https://github.com/Genymobile/scrcpy/blob/v4.1/doc/camera.md), [프로토콜](https://github.com/Genymobile/scrcpy/blob/v4.1/doc/develop.md): 버전4.1 고정 사용.
- [Android CameraCharacteristics](https://developer.android.com/reference/android/hardware/camera2/CameraCharacteristics): K/왜곡 계수 정의.

## 저장 위치

- 휴대폰 작업 동결 백업: `vision_assembly/checkpoints/phone_camera_20260906_213004`
- 백업 파일별 SHA-256은 해당 폴더의 `manifest.json`, 검증 결과는 `verification.json`.
- 앞선 전체 조립 동결 백업은 별도 보존했다.

---

## 앞서 작성된 당일 기록(보존)

## 통합 실행기 전체 최신화

- 사용자 요청에 따라 통합 실행기·사용 문서·Codex 인계를 현재 구성으로 정리했다.
- 어제 최종 인계는 VRM03 보유 후보/abnormal_stop=1 정지. 실제 현재 보유는 미확인.
  VRM 분기 고정 및 PM03·04 보정 검증은 미완료이며 이번 변경에서 수정하지 않았다.
- 최초 check: 관리7개 STOPPED, TCP10000 미수신. 유효 로봇 상태 샘플 미확보.
  사용자가 경험한 원래 오류 문구는 전달받지 못했으므로 동일 원인 해결을 단정하지 않는다.
- preflight 추가: 실행 경로 및 ROS 패키지 점검. 현재 PC 통과.
- 정지 확인 실패 시 EXIT trap이 기존 스택을 종료하지 않도록 실패 정리 활성화 시점을 수정.
- 드라이버 존재/정지 상태 미확보 시 정리 거부. --camera none에서 관리·외부 카메라 보존.
- check에 유효 상태 미확보 사유 및 구성요소 중단 실패 반환 추가.
- view에 데스크톱·RQT 패키지·통합 영상 publisher 사전 확인 추가.
- 기본 RGB/Depth1280×720·15fps, SMD set1 및 기판 기본 GPU01 선택을 문서화.
- 검증: bash -n 통과, scripts/tests/test_assembly_stack.py 7 passed, 설치 preflight 통과.
- 전체 start/restart, RQT 실행, 로봇/그리퍼 명령 및 오류 해제는 실행하지 않았다.
  실제 기동·영상 연결 검증은 아직 미완료다.

## RQT view publisher 오판 수정

- 사용자가 전체 start/check 성공 로그를 제공했다. 7개 RUNNING, TCP10000 정상,
  수동모드/정지/주요 오류0, 그리퍼26. 이는 제공된 시점의 상태다.
- view 실패 원인을 실시간 재현: publisher1이나 ros2 topic info | grep -q의
  조기 파이프 종료로 ROS CLI120/grep0 반환, pipefail이 실패로 처리했다.
- topic info 전체 출력을 수집한 뒤 검사하도록 수정. node/port 준비 검사도
  같은 조기 종료 패턴을 제거했다. 회귀시험9개와 쉘 문법 검사 통과.
- 수정 후 view 실행 및 통합 compressed 토픽 연결을 조회했다.
- 스택 재시작·로봇 이동·그리퍼 명령 없음.

## VRM03 재개 준비 — 방향 분기 수정

- 사용자: 현재 빈 그리퍼이며 VRM03부터 재개 요청. 후속 `어있어` 응답으로
  VRM03이 트레이에 존재함을 확인. 기판 VRM03 빈 슬롯과 주변 상태는 재개 전 확인 필요.
- 현재 조회: TCP 약[-15.891,-536.314,286.508,179.996,0.015,179.103],
  수동모드1, Tool1/User0, 정지1, 그리퍼26/피드백유효, abnormal/emg/main/sub/collision0.
- full_cycle_plan에서 black_block(VRM)의 배치 선호C180을 강제 분기 선택에 포함.
  기존 최대회전95도 유지, 초과 시 반대 분기로 대체하지 않고 거부한다.
- 실행기의 기존 HBM 방향 일관성·축 정렬·C180 분기 검사를 VRM에도 적용했다.
  기존 snapshot/계획에 있는 반대 방향도 실행 전 거부한다.
- 과거 실패 조건 파지C87.087/기판각0.563 재현 시험은 +93.476도 성공 분기 선택.
  회전한도 초과 거부, 반대 분기 실행 거부 포함 관련45개 시험 통과.
- 새 보드/트레이 캡처·실기 IK·물리 안착 검증은 아직 수행하지 않았다.
  로봇·그리퍼·모드 전환·오류 해제 명령 없음. 자동모드와 현장 상태 확인 후
  새 촬영과 VRM03 단품 경로 사전검사부터 진행한다.

## VRM03 반환 위치 재확인 및 단품 배치 시퀀스 완료

- 사용자가 부품 이동을 알리고 재확인 요청. 새4프레임 VRM3개가 과거3·4·5셀에
  대응함을 확인. VRM03 기존 셀 XY차이4.32mm,04 0.45mm,05 0.37mm.
  기존5mm 및 pixel12/모호성 기준 통과. 품질 기준 유지.
- 빈 그리퍼·자동모드 및 VRM03부터 재개 요청에 따라 새 좌표 단품 계획 생성.
  파지TCP[-747.856,-180.872,-53.238,-180,0,86.078],
  배치TCP[-16.378464,-516.780946,87.245329,-180,0,-179.252383].
  배치C180 대칭 분기, 계획회전+94.6696도. 기존 총95도 제한 유지.
- 파지 후 셀 제거 검증용 reference에는 남은 VRM03/04/05 모두 보존.
  vrm03_ready_snapshot/plan/run_20260906.json 및 준비 스크립트 사본 저장.
- dry-run 및 실행직전22개 경유점 검사 통과. 최대관절구간44.302도,
  J6[-14.188,9.796]도. global40/travel25/vertical10 계약 사용.
- VRM03 파지/100mm상승/TrayHome 셀 제거 확인/배치/해제/100mm 후퇴 완료.
  run 상태 motion_complete_awaiting_physical_verification, 완료목록 VRM-03.
- 실제 중심·방향·안착 성공은 작업자 확인 전까지 주장하지 않는다.
  VRM04/05는 실행하지 않았다. 로봇은 배치점 약100mm 상공에서 정지.
