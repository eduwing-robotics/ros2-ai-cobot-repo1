# 2026-08-26 작업일지

## 트레이 부품 인식 개선

- YOLO 세그멘테이션 pilot_02 학습 결과를 병합 검출에 적용.
- 붙은 부품 분리를 위한 인스턴스 마스크 후처리 적용.
- 작은 부품 테두리를 얇게 표시하고 중심점을 흑백 십자선으로 통일.
- 검출 콜백에 non-blocking lock 적용: 처리 중 들어온 프레임은 버리고 최신 DDS 프레임만 처리.
- 프레임 처리 예외가 발생해도 노드 전체가 종료되지 않고 다음 프레임을 계속 처리하도록 복구.
- Power Module 색상 범위를 H 2~42, S >= 40, V >= 55로 완화하고 기존 형상 필터 유지.
- 기본 처리 속도는 안정성을 위해 2 Hz.

## 전체 재실행

```bash
cd /home/juchan-yoon/FR5_robot_control
./vision_assembly/run_tray_merged_detection.sh
```

출력 화면 토픽: `/vision/tray/detections_image/compressed`
결과 JSON: `vision_assembly/data/tray_detections_last.json`

## 다음 확인

1. Power Module 8개가 각각 검출되는지 확인.
2. 두 개 이상 맞붙인 상태에서 개별 중심점과 테두리가 분리되는지 확인.
3. 검출 터미널에 `Detection frame failed`가 반복되면 오류 전문을 보존.

## 6클래스 세그멘테이션 학습 완료

- 새 라벨: Power Module 160개, GPU 40개, HBM 320개; 유효성 오류 0개.
- pilot_03 최적 모델: epoch 61, mask mAP50 0.995, recall 0.986, mask mAP50-95 0.838.
- 실시간 검출 기본 모델을 pilot_03 best.pt로 변경.
- 클래스별 라벨 중앙 면적을 붙은 윤곽 분리 기준으로 적용.
- 기본 confidence 0.20, 각 ROI 후보는 설계상 최대 개수로 제한.

## 실행 안정화 및 오류 수정

- 카메라 원본 토픽은 살아 있었지만 검출 노드가 종료되어 출력 Publisher가 0이 되는 현상을 확인.
- GPU 추론 콜백이 겹치지 않도록 non-blocking lock을 추가해 처리 중 들어온 오래된 프레임을 폐기.
- 프레임 단위 예외를 잡아 오류 한 번으로 검출 노드 전체가 종료되지 않도록 변경.
- YOLO 마스크의 float32 좌표를 OpenCV용 int32 윤곽으로 변환하여 `fillPoly` assertion 오류 수정.
- float32 붙은 윤곽 회귀 테스트: 2개 조각, 모두 int32로 정상 분리.
- Ctrl+C 종료 시 섹션 추적기에 표시되던 `ExternalShutdownException` traceback 제거.
- 통합 실행 스크립트는 unbuffered 출력과 기본 2 Hz 처리로 정리.

## 표시 및 집계 개선

- SMD와 작은 부품의 윤곽선을 얇게 표시.
- 모든 부품 중심점을 배경과 무관하게 보이는 흑백 이중 십자선으로 통일.
- 같은 프레임에서 가까운 SMD 두 개가 같은 트랙으로 합쳐져 10개가 9개로 표시되던 문제 수정.
- 프레임 간 오래된 윤곽을 섞어 유령 부품이 생기지 않도록 현재 프레임 중심으로 집계.
- 검출 후보 개수는 각 트레이 구역의 설계상 최대 개수로 제한.

## 데이터 및 학습 작업

- 기존 tray_parts_01 전체 원본 80장에서 Power Module, GPU, HBM 크롭을 각각 80장씩 추가 생성.
- 기존 클래스 번호와 라벨 보존: 0 VRM, 1 Inductor, 2 SMD Capacitor.
- 신규 클래스: 3 Power Module, 4 GPU, 5 HBM.
- 이번에 추가 라벨링한 20장 기준 인스턴스: Power Module 160개, GPU 40개, HBM 320개.
- 전체 라벨 좌표 및 클래스 유효성 검사 결과 오류 0개.
- 여섯 클래스가 학습/검증에 모두 포함되도록 프레임 그룹 분할 로직 개선.
- pilot_02 가중치에서 여섯 클래스 pilot_03으로 전이 학습.
- pilot_03은 91 epoch에서 조기 종료, 최적 결과는 epoch 61.
- 최적 검증 결과: mask mAP50 0.995, recall 0.986, mask mAP50-95 0.838.
- 실시간 기본 모델을 `models/tray_segmentation/pilot_03/weights/best.pt`로 변경.

## 오늘 확인된 한계

- GPU, Power Module, HBM의 새 라벨 20장은 동일 촬영 세션의 연속 프레임이라 구도와 배치가 지나치게 유사함.
- 따라서 검증 점수는 높지만 실제 회전, 위치 변화, 서로 붙은 배치에 대한 일반화는 충분하지 않음.
- 현재 분리된 Power Module 한 개는 후보 신뢰도가 0.0603이라 공통 threshold 0.20에서 누락되는 것을 확인.
- Power Module에만 threshold 0.05를 적용하고 다른 클래스는 0.20 유지.
- 임계값 완화는 현재 누락을 보완하지만, 붙은 상태의 근본 해결은 다양한 hard-example 재촬영과 재학습이 필요함.

## 2026-08-27 작업 계획

1. GPU, Power Module, HBM만 새 세션으로 별도 촬영한다.
2. 같은 화면을 반복 촬영하지 말고 매 프레임마다 위치와 회전을 크게 변경한다.
3. 각 클래스에 대해 완전히 떨어짐, 살짝 맞닿음, 2~3개 붙음, 여러 방향으로 붙음 상태를 고르게 포함한다.
4. 손이 화면에서 완전히 빠지고 부품과 카메라가 안정된 뒤 저장되게 한다.
5. 가능하면 조명과 카메라 관찰 각도도 안전 범위 안에서 조금씩 변화시킨다.
6. 새 세션을 SAM으로 라벨링할 때 붙은 부품도 반드시 인스턴스 하나당 마스크 하나로 나눈다.
7. 기존 6클래스 데이터에 새 hard examples를 합쳐 pilot_04를 학습한다.
8. 실제 라이브 화면에서 분리/접촉 배치를 모두 시험하고 클래스별 threshold를 다시 보정한다.

### 내일 촬영 시작 명령

```bash
cd /home/juchan-yoon/FR5_robot_control
./vision_assembly/run_collect_segmentation_dataset.sh --session tray_parts_02 --parts long_orange gpu hbm --max-frames 80
```

촬영 완료 후 라벨링 명령:

```bash
cd /home/juchan-yoon/FR5_robot_control
./vision_assembly/run_sam_annotation.sh --session vision_assembly/datasets/tray_segmentation/raw/tray_parts_02 --only-incomplete --limit 0
```

## 부품별 그리퍼 레시피 저장

- 사용자 실물 시험값을 `vision_assembly/config/part_gripper_recipes.json`에 저장.
- `part_specs_candidate.json`의 `gripper_recipes_file`에서 레시피 파일을 연결.
- 공통 인자 순서: index, position, velocity, force, max_time_ms, block, type, rotation_count, rotation_velocity, rotation_torque.
- GPU: grip 65 / release 70, velocity 50, force 30.
- HBM: grip 18 / release 25, velocity 50, force 30.
- Power Module: grip 26 / release 30, velocity 50, force 30.
- VRM 가로: grip 24 / release 26; 세로: grip 32 / release 34, velocity 50, force 30.
- Inductor: grip 16 / release 20, velocity 50, force 30.
- SMD Capacitor: grip 5 / release 8, velocity 50, force 1.
- 모든 명령의 max_time은 3000 ms이며 나머지 후속 인자는 0.
- 이번 작업은 값 저장만 수행했으며 로봇이나 그리퍼 명령은 실행하지 않음.

## VRM 단일 부품 100 mm Hover 시험 준비

- 시험 범위: VRM 중심 위로 이동, 검출 장축에 그리퍼 tool_y 축 정렬, 부품 상단 100 mm에서 정지.
- 표면 하강, 그리퍼 닫기, 집기, 조립 위치 이동은 포함하지 않음.
- 안전 조건: 트레이 내 VRM instant/stable 검출이 각각 정확히 1개여야 함.
- 6개 연속 프레임의 Base XYZ 최대 span 2 mm, 회전각 span 8 deg 이하만 허용.
- 트레이 TRACKING, 로봇 상태 기반 Base 변환 VALID, VRM confidence 0.70 이상 필요.
- 기존 접근기의 active tool, AUTO mode, emergency stop, stationary, 최대 이동거리 450 mm 검사를 재사용.
- 이동 속도 15%, 수직 및 회전 속도 10%, 안전 상승 여유 100 mm.
- 기본 명령은 dry-run이며 실제 이동은 `--execute --confirm-hover` 두 플래그가 모두 있어야 함.
- 도구: `vision_assembly/run_single_vrm_hover_test.sh`.

## VRM 단일 부품 Hover 실기동 결과

- 트레이에 VRM 1개만 남긴 상태에서 단일 대상 안전 조건을 충족함.
- instant VRM 1개, stable VRM 1개, 신뢰도 약 0.971.
- 6개 연속 프레임으로 목표를 확정했으며 위치 흔들림 최대 약 0.99 mm, 회전각 흔들림 0 deg.
- 카메라 중심/깊이를 Hand-Eye 변환으로 FR5 Base 좌표로 변환.
- VRM 장축 방향에 맞춰 그리퍼 tool_y 축을 회전 정렬.
- Tool 중심 보정값을 적용한 후 VRM 상단 100 mm hover 위치까지 정상 이동한 것으로 사용자 확인.
- 표면 하강, 그리퍼 닫기, 부품 집기 및 조립 이동은 실행하지 않음.

## 트레이 검출 깜빡임 원인 및 최적화

- 카메라는 약 15 FPS, 트레이 등록은 약 4 FPS로 정상인데 6개 ROI를 YOLO에 순차 6회 추론하여 검출 프레임이 약 3초까지 지연됨.
- 등록 정보 age가 13~15초로 늘면서 TRACKING/NOT_REGISTERED가 반복된 것이 깜빡임 원인.
- 여섯 ROI를 단일 GPU batch 추론으로 변경하여 호출을 6회에서 1회로 축소.
- 오프라인 측정 기준 6크롭 batch 추론 약 0.022초.
- ROS MultiThreadedExecutor를 2개에서 4개 스레드로 늘려 등록 및 로봇 상태 콜백 지연 완화.
- 최적화된 검출기 재실행 후 트레이 표시가 안정되고 VRM hover 실기동까지 완료.

## 내일 이어서 할 작업

1. GPU, Power Module, HBM을 다양한 위치·회전·간격·접촉 상태로 별도 촬영.
2. 동일 프레임 반복을 피하고 hard-example 중심으로 데이터 구성.
3. 붙은 부품도 인스턴스별로 각각 SAM 라벨링.
4. 기존 6클래스 데이터와 합쳐 pilot_04 재학습.
5. 클래스별 분리/접촉 검출을 실시간 검증하고 confidence 재조정.
6. VRM hover 재현성을 여러 위치와 회전에서 반복 검증한 뒤, 이상이 없을 때만 다음 단계의 저속 하강 시험을 별도 승인 후 진행.
