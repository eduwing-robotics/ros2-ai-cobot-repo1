# KSMC workspace instructions

## 작업 기록

- 비전·AI 기능을 새로 만들거나 동작 기준을 바꾸면 완료한 같은 작업에서
  `Vision_AI_작업기록.md`와 `docs/logs/vision.md`를 갱신한다.
- 상세 기록은 관련 작업을 묶어 `docs/logs/vision.md`에 영어로 한 번만 작성한다.
  `Vision_AI_작업기록.md`에는 최신 상세 기록 링크만 갱신하고 번역·중복 기록하지 않는다.
  기존 기록은 보존하며 사용자가 요청하면 그때 한국어로 정리해 설명한다.
- 카메라 전송·화질·연결 변경은 `docs/logs/camera.md`, 컨베이어 검출·정지·자동
  트리거 변경은 `docs/logs/conveyor.md`에도 함께 기록한다.
- 기록에는 최종 동작, 근거가 된 실측/데이터, 검증 결과, 로봇·컨베이어 실제 명령
  여부, 남은 제한사항을 적는다. 실패한 임시 결과를 최종값처럼 기록하지 않는다.

## 비전 검사 고정 아키텍처

- 검사 기능을 만들거나 판정 기준을 바꾸기 전에
  `vision_assembly/config/inspection_fusion_contract.json`을 확인하고, 그 계약의
  S22 독립형 하이브리드 조합과 fail-safe 융합 규칙을 유지한다.
- 팀원의 D435 학습 모델을 필수 의존성으로 만들지 않는다. 미검증·저신뢰 모델은
  `ADVISORY_ONLY`이며 단독 PASS/FAIL 권한을 주지 않는다.
- FR-PatchCore 또는 다른 정합 모델이 슬롯 상대 위치·방향 오류를 정상 변화로
  제거하게 만들지 않는다. 계약 자체를 변경해야 할 때는 사용자에게 변경 이유와
  영향 범위를 먼저 명시하고 작업 기록도 함께 갱신한다.
