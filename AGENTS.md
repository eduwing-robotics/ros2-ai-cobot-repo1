# 프로젝트 작업 정책

## 범위와 변경

- 사용자 설계, 현재 명령, 요청 범위가 이 문서보다 우선한다. 범위 밖 구조 개선이나 대안을 선제적으로 제안하지 않는다.
- 기존 코드, Unity/ROS 기본 기능, 표준 기능, 설치된 의존성 순으로 재사용한다. 미래 확장용 추상화·래퍼·설정·보일러플레이트는 만들지 않는다.
- 신규 의존성, 공개 API, 폴더·네임스페이스, Scene/Prefab 계층, ROS 토픽·메시지 계약은 사용자 승인 없이 바꾸지 않는다.
- 가장 적은 파일과 최소 변경으로 해결한다. 단, 입력 검증, 데이터 손실 방지, 보안, 하드웨어 안전, 명시 요구사항은 생략하지 않는다.

## 조사와 검증

- 구현 전 활성 Runtime 경로와 모든 실제 호출자를 확인한다. Legacy는 실제 참조·호출될 때만 조사한다.
- 새 스크립트를 만들기 전에 이름·공개 타입·책임·호출자를 검색해 기존 소유자를 찾는다. 같은 책임의 MonoBehaviour·서비스·래퍼를 중복 생성하지 않는다.
- 변경은 기능의 공개 진입점과 상태를 소유한 기존 컴포넌트에 둔다. 책임이 불명확하면 후보 파일과 호출 흐름을 먼저 보고하며 새 스크립트로 우회하지 않는다.
- 목표 설계와 현재 구현을 구분한다. Real 자동 조립 등 미구현 기능을 구현됐다고 가정하지 말고 source와 caller로 확인한다.
- 버그는 모든 호출자가 지나는 가장 낮은 공통 경계의 근본 원인을 고친다. 공개 계약·구조·범위를 바꿔야 하면 먼저 영향과 최소 대응을 보고한다.
- 변경 후 diff를 검토하고 가능한 빌드·테스트·정적 검사를 실행한다. 검증하지 못한 항목은 사유와 범위를 포함해 미검증으로 보고한다.

## 작업 등급과 권한

- 읽기, 설명, 계획, 비파괴 조사는 상위 모델이 직접 수행한다.
- 제품 코드와 Unity Asset 수정은 구현 서브에이전트가 맡고, 위험도에 맞는 모델과 추론 수준을 선택한다. 상위 모델은 최종 diff와 실제 검증 결과를 직접 확인한다.
- 병렬 쓰기 또는 Scene/Prefab, ROS·하드웨어, 공개 계약, 복수 파일의 고위험 변경만 전용 브랜치·worktree와 high 추론을 사용한다.
- 단순한 기존 C# 소수 파일 수정은 현재 worktree에서 구현 서브에이전트 한 명만 쓰도록 허용한다. 충돌 가능한 파일·Scene·실행 상태는 순차 처리한다.
- 구현 서브에이전트는 지정된 기존 파일과 사용자 요청·승인에 포함된 신규 경로만 수정하거나 생성한다. 제품 코드나 Unity Asset을 수정하지 않는 상위·검증 에이전트는 구현 worktree에 쓰지 않는다.

## 파일과 산출물

- 신규 파일은 요청에 포함됐거나 기존 파일 수정으로 불가능할 때만 만든다. 후자는 정확한 경로, 단일 책임, 예상 호출자·연결 위치, 기존 소유자로 처리할 수 없는 이유와 영향을 보고해 사용자 승인을 받는다.
- 승인되지 않은 신규 파일, 스크립트, 폴더, 링크, 테스트 데이터, 설정, Unity Asset, `.meta` 파일을 만들지 않는다.
- Git에서 무시되는 Unity 빌드 캐시와 임시 산출물은 검증에 필요한 범위에서 허용한다. 명령이 지원하면 저장소 밖 임시 경로를 사용하고, 산출물을 Git에 추가하지 않는다. 작업 전후 변경·미추적 파일을 비교한다.

## 로봇 계층 계약

- Scenario와 GUI는 주입된 인터페이스만 사용하며 구체 Mock/Real 구현 참조·캐스팅·반복 lookup을 하지 않는다.
- RobotMaster만 Mock/Real backend를 선택하고 `IRobotScenarioControl`을 Scenario에, `IRobotControl`을 수동 조작자에 주입한다. RobotMaster에 변환·ROS·실행 로직을 넣지 않는다.
- 자동 조립은 `IRobotScenarioControl.ExecuteAsync()`로만 요청하고, 수동·저수준 조작은 `IRobotControl`만 사용한다. 기존 두 계약으로 표현할 수 없을 때만 새 계약을 추가한다.
- `ExecuteAsync()`의 성공은 실제 작업 완료다. 요청 수락을 완료로 반환하지 않으며 실패와 timeout은 호출자에게 전달한다.
- 오케스트레이터는 업무 흐름·다음 선택·중단/건너뛰기/재시도 정책만 표현한다. 좌표·단위·프로토콜 변환, ROS 메시지, 입력 검증, 하드웨어 제어·상태 판독은 넣지 않는다.
- 하위 공개 진입점은 입력·필수 참조·안전 조건을 검증하고, 변환·ROS 통신·실제 완료 감지·timeout·정리·실패 전달을 완결한다. 공유 검증과 상태 판정은 공통 경계에서 한 번만 처리한다.
