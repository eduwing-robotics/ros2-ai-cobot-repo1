# 프로젝트 작업 정책

## 범위와 최소 변경

- 사용자 설계, 현재 명령, 요청 범위가 우선한다. 범위 밖 구조 개선이나 대안을 선제적으로 제안하지 않는다.
- 구현 전 활성 Runtime 경로, 공개 진입점, 상태 소유자와 모든 실제 호출자를 확인한다. Legacy는 실제 참조될 때만 조사한다.
- 기존 코드, Unity/ROS 기본 기능, 표준 기능, 설치된 의존성 순으로 재사용한다.
- 미래 확장용 추상화, 래퍼, 설정, 보일러플레이트와 같은 책임의 중복 컴포넌트를 만들지 않는다.
- 버그는 모든 호출자가 지나는 가장 낮은 공통 경계에서 고친다. 신규 컴포넌트로 우회하지 않는다.
- 가장 적은 파일과 최소 변경으로 해결한다. 입력 검증, 데이터 손실 방지, 보안, 하드웨어 안전과 명시 요구사항은 생략하지 않는다.
- 공개 API, 폴더·네임스페이스, Scene/Prefab 계층, ROS 토픽·메시지 계약은 사용자 승인 없이 바꾸지 않는다.

## 신규 스크립트와 라이브러리

- 신규 스크립트 파일, 공용 라이브러리 코드, 외부 라이브러리·패키지·의존성의 생성·추가·교체는 비용이 높은 예외로 취급한다.
- 현재 요청에 신규 생성이 포함되거나 필요해 보여도 이를 승인으로 간주하지 않는다. 실행 전에 사용자에게 별도의 명시적 승인을 받고 응답을 기다린다.
- 승인 요청에는 정확한 이름과 경로, 단일 책임, 예상 호출자와 연결 위치, 기존 소유자·기본 기능·설치된 의존성으로 해결할 수 없는 이유, 영향 범위를 포함한다.
- 승인 전에는 관련 폴더, 설정, 테스트 파일, Unity Asset과 `.meta` 파일도 생성하지 않는다. 승인은 보고한 경로와 범위에만 적용한다.

## 조사와 검증

- 목표 설계와 현재 구현을 구분하고, 구현 여부를 source와 caller로 확인한다.
- 변경 후 diff와 작업 전부터 존재하던 변경을 구분해 검토하고 가능한 빌드·테스트·정적 검사를 실행한다.
- 검증하지 못한 항목은 사유와 미검증 범위를 보고한다.

## 로봇 계층 계약

- Scenario와 GUI는 주입된 인터페이스만 사용하며 구체 Mock/Real 구현 참조, 캐스팅과 반복 lookup을 하지 않는다.
- RobotMaster만 Mock/Real backend를 선택하고 `IRobotScenarioControl`을 Scenario에, `IRobotControl`을 수동 조작자에 주입한다. 변환, ROS와 실행 로직은 넣지 않는다.
- 자동 조립은 `IRobotScenarioControl.ExecuteAsync()`로만 요청하고 수동·저수준 조작은 `IRobotControl`만 사용한다. 기존 계약으로 표현할 수 없을 때만 사용자 승인 후 새 계약을 추가한다.
- `ExecuteAsync()`의 성공은 실제 작업 완료를 뜻한다. 요청 수락을 완료로 반환하지 않으며 실패와 timeout을 호출자에게 전달한다.
- 오케스트레이터는 업무 흐름과 중단·건너뛰기·재시도 정책만 표현한다. 변환, ROS 메시지, 입력 검증, 하드웨어 제어와 상태 판독은 넣지 않는다.
- 하위 공개 진입점이 검증, 변환, 통신, 실제 완료 감지, timeout, 정리와 실패 전달을 완결한다. 공유 검증과 상태 판정은 공통 경계에서 한 번만 처리한다.
