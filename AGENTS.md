# 프로젝트 작업 정책

## 설계 주도권

- 시스템 설계, 구조, 경계, 기술 선택의 최종 결정권은 사용자에게 있다.
- 사용자가 지정한 설계를 기본값으로 삼고 요청된 범위만 구현한다.
- 사용자의 현재 작업 명령이 이 문서의 기본 정책보다 구체적이면 현재 명령을 우선한다.
- 더 나은 구조, 대안 설계, 확장 방향을 선제적으로 추천하지 않는다.
- 설계 관련 조언은 현재 코드 작성으로 컴파일 오류, 런타임 결함, 데이터 손실, 보안 문제, 명백한 성능 문제, 플랫폼 비호환 또는 요구사항 충돌이 발생할 가능성이 있을 때만 제공한다.
- 문제 가능성을 알릴 때는 구체적인 영향과 가장 작은 대응책만 설명한다. 선택이 결과를 크게 바꾸는 경우에만 사용자 결정을 요청한다.

## 변경 비용

- 신규 라이브러리와 외부 의존성 추가는 높은 비용의 변경으로 취급하며 사용자 승인 없이 추가하지 않는다.
- 기존 프로젝트 코드, Unity/ROS 기본 기능, 표준 기능, 현재 설치된 의존성 순서로 우선 사용한다.
- 폴더 구조, 네임스페이스, 공개 API, 씬·프리팹 계층, ROS 토픽·메시지 계약 변경도 높은 비용으로 취급한다.
- 사용자가 명시하지 않은 구조 변경, 파일 이동, 추상화, 계층 추가는 하지 않는다.
- 미래 확장을 위한 인터페이스, 팩토리, 설정, 래퍼 또는 보일러플레이트를 미리 만들지 않는다.
- 필요한 기능을 충족하는 가장 작은 변경과 가장 적은 파일 수를 우선한다.

## 작업 방식

- 구현 전 관련 코드와 호출 흐름을 확인하고 기존 기능을 재사용한다.
- 버그는 증상이 아니라 모든 호출자가 공유하는 근본 원인 위치에서 수정한다.
- 근본 원인 수정에 공개 계약, 구조 또는 요청 범위 변경이 필요하면 수정 전에 사용자에게 보고한다.
- 입력 검증, 데이터 손실 방지, 보안, 하드웨어 안전과 명시된 요구사항은 단순화를 이유로 생략하지 않는다.
- 변경 후 실제 diff를 검토하고 가능한 빌드, 테스트 또는 정적 검사를 실행한다.
- 검증하지 못한 항목은 완료된 것처럼 표현하지 않고 사유와 미검증 범위를 보고한다.
- 요청 범위를 넘어서는 개선 사항은 별도 작업으로 만들거나 권하지 않는다.

## 모델과 서브 에이전트 역할

- 상위 모델은 사용자 대화, 요구사항 해석, 설계·기획, 작업 분해, 최종 코드 리뷰, 검증과 결과 보고를 담당한다.
- 상위 모델은 제품 코드와 Unity Asset을 직접 수정하지 않는다.
- 서브 에이전트를 사용할 수 없는 경우에는 사용자에게 보고하고 명시적으로 승인받은 작업만 상위 모델이 직접 수행한다.
- 코드 작성·수정, 구현과 반복적인 조사·실행 작업은 하위 서브 에이전트가 담당한다.
- 모든 구현·조사·검증 서브 에이전트는 `gpt-5.6-terra` 모델과 `high` 추론 수준으로 호출한다.
- 상위 모델은 서브 에이전트 결과를 그대로 수용하지 않고 변경 diff와 실제 실행 결과를 직접 검토해 최종 판단한다.
- 하위 서브 에이전트가 수행한 테스트는 참고할 수 있지만 최종 검증과 완료 판정은 상위 모델이 직접 수행한다.
- 서로 독립적이고 파일 또는 실행 상태가 충돌하지 않는 작업이 둘 이상이면 동시에 서브 에이전트에 배정한다.
- 병렬 작업 전 각 서브 에이전트의 책임 범위, 변경 파일과 결과물을 분리한다.
- 선행 결과가 필요하거나 동일 파일·씬·실행 프로세스를 변경해 충돌할 수 있는 작업은 순차 수행한다.
- 병렬화를 위해 단일 작업을 억지로 분해하거나 중복 구현·검증을 만들지 않는다.

## 워크트리와 쓰기 권한

- 구현 작업은 작업 ID별 전용 Git 브랜치와 워크트리에서 수행한다.
- 하나의 워크트리에는 하나의 구현 서브 에이전트만 쓰기 작업을 수행한다.
- 상위 모델과 검증 에이전트는 구현 워크트리의 제품 코드와 Unity Asset을 수정하지 않는다.
- 구현 서브 에이전트는 자신에게 배정된 워크트리와 수정 허용 파일만 변경한다.
- 상위 모델은 작업 시작 전 기준 커밋과 Git 변경 상태를 기록하고 작업 후 diff와 미추적 파일을 비교한다.
- 상위 모델은 검토와 검증이 끝난 구현 결과만 병합하거나 적용하며, 병합 과정에서 제품 코드를 직접 고치지 않는다.
- 병합 충돌로 기능 코드 수정이 필요하면 해당 영역의 구현 서브 에이전트에게 다시 배정한다.

## 서브 에이전트 파일 생성 제한

- 하위 서브 에이전트는 상위 모델이 작업별로 지정한 기존 파일만 수정할 수 있다.
- 하위 서브 에이전트는 신규 파일, 스크립트, 폴더, 심볼릭 링크, 테스트 데이터, 설정 파일, Unity Asset과 `.meta` 파일을 생성하지 않는다.
- Git에서 무시되는 빌드 캐시와 임시 산출물은 검증에 필요한 경우에만 허용하며 Git에 추가하거나 최종 결과물로 취급하지 않는다.
- 빌드·테스트·코드 생성 명령이 저장소 안에 신규 산출물을 만들 수 있으면 `/tmp` 같은 저장소 밖의 임시 경로를 사용한다.
- 구현에 신규 파일이 필요하면 생성하지 않고 상위 모델에 다음 내용을 보고한다.
  - 생성이 필요한 정확한 경로
  - 파일의 역할과 필요 이유
  - 기존 파일 수정으로 처리할 수 없는 이유
  - 예상되는 호출자와 영향 범위
- 상위 모델은 신규 파일 생성안을 사용자에게 전달하고 명시적 승인을 받은 뒤에만 승인된 경로를 하위 서브 에이전트의 작업 범위에 추가한다.
- 신규 파일 승인은 제안된 정확한 경로에만 적용하며 같은 폴더의 다른 파일 생성을 허용하지 않는다.
- 상위 모델은 하위 작업 전후의 Git 추가·미추적 파일 목록을 직접 비교해 승인되지 않은 신규 파일이 없는지 확인한다.
- 승인되지 않은 신규 파일이 발견되면 작업을 완료로 판정하지 않고 사용자에게 생성 경로와 발생 원인을 보고한다.

## 계층 책임

- 상위 오케스트레이터는 상위 업무 흐름과 업무 의도만 표현한다.
- 하위 컴포넌트는 자신이 제공하는 공개 함수의 실행을 완결해서 책임진다.
- 호출자가 하위 컴포넌트의 내부 구현을 알고 사전 처리하게 만들지 않는다.
- 여러 호출자가 공유하는 검증과 안전 처리는 모든 호출이 통과하는 가장 낮은 공통 경계에서 한 번만 구현한다.
- 하위 컴포넌트는 실패를 로그만 남기고 숨기지 않는다.
- 성공, 실패와 실패 원인을 공통 계약에 따라 호출자에게 전달한다.

## 오케스트레이터 코드 스타일

- 오케스트레이터의 각 작업 호출은 업무 의도가 드러나는 한 줄로 작성한다.
- 한 줄에는 하나의 의미 있는 작업만 표현한다.
- 좌표 계산, 좌표계 변환, 프로토콜 변환, ROS 메시지 생성, 입력값 검증과 하드웨어 제어 세부사항을 오케스트레이터에 작성하지 않는다.
- 오케스트레이터는 통신 완료 여부, 타임아웃과 구현별 상태 메시지를 직접 판독하지 않는다.
- 상위 업무 흐름, 다음 업무 선택, 업무상 중단·건너뛰기·재시도 정책은 오케스트레이터가 책임진다.
- 한 줄 호출을 만들기 위한 의미 없는 래퍼는 추가하지 않는다.
- 호출하는 함수나 명령 이름 자체가 업무 의도를 설명해야 한다.

호출 예시:

```csharp
await assembly.ExecuteAsync();
```

## 하위 컴포넌트 코드 스타일

- 하위 컴포넌트는 공개 진입점에서 자신의 입력과 실행 조건을 검증한다.
- 하위 컴포넌트는 자신의 책임 범위에 해당하는 다음 요소를 처리한다.
  - 입력값과 필수 참조 검증
  - 공통 및 구현별 안전 조건 확인
  - 좌표와 단위 변환
  - ROS 토픽·서비스 메시지 생성과 통신
  - 명령 수락과 실제 완료 구분
  - 실패 응답과 타임아웃 처리
  - 작업 중 사용한 상태와 자원 정리
- `ExecuteAsync`가 성공으로 완료되면 요청한 작업이 실제로 완료된 상태여야 한다.
- 요청 발행 성공을 실제 작업 완료로 반환하지 않는다.
- 구현 내부 오류는 공통 결과 또는 예외 계약을 통해 상위 호출자에게 전달한다.

## Mock/Real 의존성 주입

- 상위 오케스트레이터는 Mock/Real을 직접 분기하지 않는다.
- 자동 조립은 주입된 `IRobotScenarioControl`만 호출한다.
- 수동 조작은 주입된 `IRobotControl`만 호출한다.
- Scenario와 GUI는 구체적인 Mock/Real 구현 클래스를 직접 참조하지 않는다.
- Scenario와 GUI에서 구체 구현체로 타입 캐스팅하지 않는다.
- Mock/Real별 통신, callback 해석, 완료 감지와 실행 방식의 차이는 주입된 구현체가 책임진다.
- 두 구현은 성공, 실패와 타임아웃의 의미를 동일하게 제공한다.
- 두 구현이 공유하는 검증과 상태 판정은 모든 경로가 통과하는 기존 공통 코드에서 한 번만 처리한다.
- Mock 전용 시각화와 가상 물체 처리는 Mock 구현 내부에 둔다.
- Real 전용 ROS 서비스와 비전 시스템 연동은 Real 구현 내부에 둔다.

호출 구조:

```text
Scenario
  └─ IRobotScenarioControl.ExecuteAsync()
       └─ RobotMaster가 선택한 구현
            ├─ mockAsyncPlay
            └─ RealAyncPlay

수동 조작 GUI
  └─ IRobotControl
       └─ RobotMaster가 선택한 Mock/Real 제어 구현
```

## RobotMaster 책임

- RobotMaster는 Mock/Real backend를 선택하는 유일한 위치다.
- RobotMaster는 선택된 `IRobotScenarioControl`을 Scenario에, `IRobotControl`을 수동 조작 호출자에 주입한다.
- RobotMaster는 Mock/Real별 좌표 변환, ROS 통신 또는 작업 실행 로직을 구현하지 않는다.
- RobotMaster 외부에 Mock/Real 모드 분기를 중복 작성하지 않는다.
- 선택된 backend가 자동 또는 수동 계약을 제공하지 못하면 초기화 단계에서 명확한 오류로 처리한다.

주입 예시:

```csharp
scenario.Initialize(ScenarioControl);
```

## 주입된 인터페이스 사용

- 주입받은 인터페이스는 짧고 의미가 명확한 필드명으로 보관한다.
- 오케스트레이터에서 인터페이스 타입명이나 구현체 이름을 반복하지 않는다.
- Unity Inspector에서 인터페이스를 직접 직렬화하지 않고 RobotMaster가 런타임에 선택된 구현체를 주입한다.
- 주입 이후 `GetComponent`, 타입 검색 또는 Mock/Real 캐스팅을 반복하지 않는다.

```csharp
IRobotScenarioControl assembly;

public void Initialize(IRobotScenarioControl selectedAssembly) =>
    assembly = selectedAssembly;
```

Scenario 내부에서는 다음처럼 사용한다.

```csharp
await assembly.ExecuteAsync();
```

## 자동 조립과 수동 제어 계약

- `IRobotScenarioControl`은 Mock과 Real의 자동 조립 실행 계약이다.
- MVP 진입점은 `ExecuteAsync()` 하나로 한다.
- `ExecuteAsync()`가 성공으로 완료되면 요청한 조립 작업이 실제로 완료된 상태여야 한다.
- 요청 수락만을 성공으로 반환하지 않고, 실패와 타임아웃은 모두 호출자에게 전달한다.
- `IRobotControl`은 작업 흐름 밖의 수동 조작과 저수준 로봇 제어에만 사용한다.
- 자동 조립 요청에 Unity `Transform`, `Pose`, 부품 순서, ROS 메시지 같은 구현 세부사항을 노출하지 않는다.
- 새 인터페이스나 명령 래퍼는 기존 두 계약으로 요구사항을 표현할 수 없을 때만 추가한다.

예상 계약:

```csharp
public interface IRobotScenarioControl
{
    Task ExecuteAsync();
}
```

## Scenario 책임 범위

Scenario가 책임지는 요소:

- 조립 작업을 요청할 시점과 상위 수준 의도
- 주입된 `IRobotScenarioControl`을 통한 조립 작업 호출
- 업무 규칙에 따른 중단, 건너뛰기 또는 다음 단계 선택
- 상위 수준 작업 진행 상태 전달

Scenario가 책임지지 않는 요소:

- Mock/Real 모드 분기
- 조립 부품, 슬롯과 스텝 순서 선택
- Unity 좌표 계산과 로봇 좌표계 변환
- ROS 메시지와 서비스 명령 생성
- 로봇 관절·TCP·그리퍼의 세부 제어 순서
- 하드웨어 안전 검사
- 명령 완료 토픽과 상태 필드 직접 판독
- Mock 가상 물체 부착과 해제
- Real 카메라 및 비전 좌표 처리
