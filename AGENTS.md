# 프로젝트 작업 정책

## 범위와 최소 변경

- 사용자 설계, 현재 명령과 요청 범위를 우선하며 범위 밖 구조 개선이나 대안을 선제적으로 적용하지 않는다. 현재 설계가 컴파일·런타임 결함, 데이터 손실, 보안·하드웨어 안전 문제, 확인된 성능 문제 또는 요구사항 충돌을 일으킬 때만 구체적인 영향과 최소 대응책을 알리며, 결과가 크게 달라질 때만 사용자 결정을 요청한다.
- 구현 전 활성 Runtime 경로, 공개 진입점, 상태 소유자와 모든 실제 호출자를 확인한다. Legacy는 실제 참조될 때만 조사한다.
- 기존 코드, Unity/ROS 기본 기능, 표준 기능, 설치된 의존성 순으로 재사용한다.
- 미래 확장용 추상화, 래퍼, 설정, 보일러플레이트와 책임이 중복되는 컴포넌트를 만들지 않는다.
- 버그는 모든 호출자가 지나는 가장 낮은 공통 경계에서 원인을 수정한다. 신규 컴포넌트로 우회하지 않는다.
- 가장 적은 파일과 최소 변경으로 해결한다. 입력 검증, 데이터 손실 방지, 보안, 하드웨어 안전과 명시 요구사항은 생략하지 않는다.
- public API, 폴더·파일 위치, 네임스페이스, Scene·Prefab 계층, 직렬화 필드, assembly definition과 ROS 토픽·서비스·액션·메시지 계약은 사용자 승인 없이 변경하지 않는다.

## 신규 스크립트와 라이브러리

- 신규 스크립트 파일, 공용 라이브러리 코드와 외부 라이브러리·패키지·의존성의 생성·추가·교체는 비용이 높은 예외로 취급한다.
- 현재 요청에 신규 생성이 포함되거나 필요해 보여도 승인으로 간주하지 않는다. 실행 전에 별도의 명시적 승인을 받고 응답을 기다린다.
- 승인 요청에는 정확한 이름과 경로, 단일 책임, 예상 호출자와 연결 위치, 기존 소유자·기본 기능·설치된 의존성으로 해결할 수 없는 이유와 영향 범위를 포함한다.
- 승인 전에는 관련 폴더, 설정, 테스트 파일, Unity Asset과 `.meta` 파일도 생성하지 않는다. 승인은 보고한 경로와 범위에만 적용한다.

## 구조와 로봇 계층 계약

- 기본 실행 구조는 `Orchestrator → Components`로 유지한다. 실제 복잡성이 발생하고 사용자가 승인한 경우에만 Task나 StateMachine 계층을 추가한다.
- Scenario와 GUI는 주입된 인터페이스만 사용하며 구체 Mock·Real 구현 참조, 캐스팅과 반복 lookup을 하지 않는다.
- RobotMaster만 Mock/Real backend를 선택하고 `IRobotScenarioControl`을 Scenario에, `IRobotControl`을 수동 조작자에 주입한다. 변환, ROS와 실행 로직은 넣지 않는다.
- 자동 조립은 `IRobotScenarioControl.ExecuteAsync()`로만 요청하고 수동·저수준 조작은 `IRobotControl`만 사용한다. 기존 계약으로 표현할 수 없을 때만 사용자 승인 후 새 계약을 추가한다.
- `ExecuteAsync()` 성공은 실제 작업 완료를 뜻한다. 요청 수락을 완료로 반환하지 않으며 실패와 timeout을 호출자에게 전달한다.
- Orchestrator는 위에서 아래로 읽히는 업무 흐름과 단계별 중단·건너뛰기·재시도 여부만 표현한다. 기존 공개 진입점을 호출하며 일회성 래퍼로 흐름을 분산하지 않는다.
- 하위 공개 진입점이 입력 검증, 변환, ROS·통신, 통신 재시도, timeout, 실제 완료 감지, 정리와 실패 전달을 완결한다. 공유 검증과 상태 판정은 공통 경계에서 한 번만 처리한다.

## Unity와 ROS 경계

- Inspector에 연결된 참조를 코드 검색이나 자동 생성으로 몰래 대체하지 않는다.
- Unity 생명주기 메서드는 생명주기 책임만 담당하며 `Update()`에 네트워크·DB 작업, 무거운 검색과 반복 할당을 추가하지 않는다.
- Unity와 ROS 좌표 변환은 하나의 기존 경계에서만 처리하며 좌표계와 단위를 명시한다.

## 코드와 주석

- 복잡한 표현과 숨은 제어 흐름을 피한다. 함수는 길이가 아니라 책임과 복잡성을 기준으로 나누며 단순한 선형 흐름은 불필요하게 분리하지 않는다.
- 먼저 명확한 이름과 제어 흐름으로 의도를 표현한다. 코드만으로 명확한 동작은 주석으로 반복하지 않는다.
- 주석은 설계 이유, 비자명한 가정, 불변 조건, 외부 제약, 실패 조건과 선택한 방식의 한계를 설명한다. 짧게 줄여 오해될 경우 필요한 맥락을 코드 가까이에 완결되게 기록한다.
- 타이밍, 상태 전이, 좌표·단위, 하드웨어 보정과 성능 절충에는 실제 경계와 위반 시 영향을 구체적으로 적는다.
- 코드 변경과 함께 주석을 갱신하거나 제거한다. 주석 처리된 코드는 남기지 않고, TODO에는 해결 조건이나 관련 이슈를 기록하며, XML 문서 주석은 public API와 외부 계약에만 사용한다.

## 조사, 검증과 보고

- 변경 후 diff와 작업 전부터 존재하던 변경을 구분해 검토한다.
- 기존 빌드·테스트·포매터·Analyzer를 실행하고 C# 컴파일, 참조·네임스페이스, Unity 직렬화, public API·ROS 계약과 요청 범위 밖 변경을 확인한다.
- 실행하지 못한 검증은 성공으로 표현하지 않고 사유와 미검증 범위를 밝힌다.
- 결과에는 변경 내용, 수정 파일, 수행한 검증과 남은 위험만 간결하게 보고한다. 요청하지 않은 후속 리팩터링이나 확장은 수행하지 않는다.
