# 2026-08-31 작업일지

## 작업 목표

- Power Module, VRM, Inductor, SMD Capacitor의 중심 보정·자세 정렬·파지 높이와 그리퍼 값을 실제 로봇으로 순차 검증.
- 모든 부품에 공통으로 적용할 안전 접근 절차를 정리하고 실행기로 통합.
- SMD의 작은 미세 회전을 근접 영상으로 재검출하되, 전체 트레이 기반 중심 좌표와 결합하는 연속 비전 흐름 구현.
- 트레이 비전, FAIRINO 상태 서버, Unity Endpoint의 실행 방식과 디지털 트윈 연동 내용을 최신화.

## 공통 안전 기준과 실행기

- 기본 복귀 위치는 항상 `TrayHome`으로 확정하고 `ClosePin`을 기본 경유점에서 제외.
- 실제 이동 전 AUTO 모드, Tool1/User0, 정지 상태, 비상정지·충돌·주요 경보, IK, 관절 소프트 리밋을 검사.
- 부품 근처 XY 이동은 반드시 현재 위치에서 수직 상승 → 안전 높이에서 자세 회전 → 수평 이동 → 수직 하강 순서로 실행.
- 카메라 검출 위치로는 우선 부품 표면 20 mm 위에서 정지하고, 작업자 중심 확인 후 Base-Z 방향으로만 최종 하강.
- 공통 2단계 파지 실행기 `vision_assembly/scripts/safe_part_pick.py` 및 런처 `vision_assembly/run_safe_part_pick.sh`를 추가.
- 부품별 레시피에서 중심 보정, 그리퍼 축, 파지 높이, 열림·닫힘 값을 불러오도록 구성.
- 자동 이동 중 잘못된 트레이 홈 또는 낮은 높이에서의 XY 이동을 차단하도록 복귀·접근 스크립트를 보강.

## Power Module 검증

- 기존 부품별 방향 정렬과 화면 오른쪽(Base +Y) 보정값을 유지하면서 추가 미세 보정을 반복 시험.
- 최종 누적 보정으로 Power Module 3번과 4번에서 중심 및 파지·상승을 재현하고 육안상 성공 확인.
- Power Module 보정값과 그리퍼/파지 높이는 `part_gripper_recipes.json`에 저장.
- 트레이의 다른 위치에서도 동일 레시피가 적용되도록 부품 공통값으로 유지.

## VRM 검증

- 초기 VRM 1번 명령이 4번 위치로 향하고 TrayHome도 잘못된 위치로 가는 위험 상황을 확인.
- 원인은 위치 선택·복귀 흐름이 공통 안전 절차를 우회할 수 있었던 것으로 판단하고, TrayHome 고정 및 단계별 접근 검사를 강화.
- VRM 중심 보정을 화면 오른쪽 방향(Base +Y)으로 반복 조정하여 최종 +1.6 mm로 저장.
- 조립용 그리퍼 값과 별도로 트레이 픽업 진입용 열림값 28을 추가.
- 부품 상단 돌출을 줄이기 위해 Base -X 2 mm 보정을 추가.
- VRM 7번과 9번에서 수정된 보정·열림값으로 진입을 재현했고, VRM 9번은 정상 파지 확인.
- 관련 검출/세션 파일: `vrm_1_hover_target.json`, `vrm_4_hover_target.json`, `vrm_7_hover_target.json`, `vrm_9_hover_target.json`.

## Inductor 검증

- 트레이 벽과의 간섭을 피하기 위해 다른 일부 부품과 달리 그리퍼를 회전한 세로 기준(`tool_y`)으로 접근하는 방식을 유지.
- 중심 보정은 Base +Y 4 mm, Base -X 1 mm로 저장.
- Inductor 1번에서 중심을 맞추고 수동으로 파지 높이를 티칭하여 저장.
- Inductor 3번 최초 시험에서 하강 중 부품 위치를 맞추는 형태로 접근해 충돌 위험이 있었음.
- 이후 공통 절차대로 안전 높이에서 XY와 회전을 먼저 완료하고 Base-Z로만 내려가도록 수정하여 3번 중심·파지를 재시험.
- 그리퍼 회전 속도를 상향 조정.

## SMD Capacitor 그리퍼·보정값

- 트레이 벽 간섭을 피하기 위해 Inductor와 같은 세로 파지 기준(`tool_y`)으로 변경.
- 중심 보정: Base X -1 mm, Base Y +4 mm.
- 픽업 진입 열림값은 시험을 거쳐 최종 17, 닫힘값은 12로 저장.
- 수동 티칭 절대 파지 TCP Z: -53.153 mm.
- 기준 검출 표면 Z: -47.291 mm, 저장 파지 오프셋: -5.862 mm.
- SMD 4번과 9번에서 중심 진입을 시험했으나 작은 부품의 약 1~2° 미세 회전을 전체 트레이 영상만으로 안정적으로 반영하기 어려운 문제 확인.

## SMD 근접 비전과 추가 학습

- SMD 영역만 가까이 보는 `SMDSectionView`를 티칭:
  - TCP/Base `[-527.998, -120.961, 77.862, 180, 0, 90]`.
- 검정 테이프 네 꼭짓점으로 SMD 섹션을 한 번 등록한 뒤 테이프를 제거하고, 저장된 섹션 기준과 학습 모델을 사용하도록 구성.
- SMD 근접 영상 데이터셋을 추가 수집하고 OBB 모델 `pilot_02`를 학습.
- 흰색 끝부분과 흰색 트레이 때문에 단순 갈색 HSV 윤곽의 각도가 틀릴 수 있어 OBB 긴 축을 최종 미세각에 사용.
- 1920 전용 카메라 재시작 방식은 전체 트레이 homography와 화면 연속성을 깨므로 사용하지 않기로 함.
- 단일 1280×720 RGB-D 스트림에서 전체 트레이와 SMD 근접 OBB가 모두 동작함을 오프라인/실시간으로 확인.
- `capture_smd_close_target.py`에 1280 영상 비율에 맞춘 SMD 섹션 폴리곤 스케일링을 추가.
- 플랜지 각도가 +180/-180° 경계를 오갈 때 자세 변화량을 360°로 오판하던 검증 버그를 원형 각도 계산으로 수정.

## SMD 전체샷 중심 + 근접각 결합

- 의도한 흐름:
  1. TrayHome 전체 영상에서 SMD 번호와 중심 XYZ를 안정적으로 저장.
  2. SMD 근접 뷰로 이동.
  3. 근접 영상에서는 XYZ를 바꾸지 않고 미세 회전각만 재검출.
  4. 안전 높이에서 `tool_y`를 검출 장축에 맞춰 회전.
  5. TrayHome에서 얻은 중심 XYZ에 저장 보정을 적용해 직접 접근.
- `merge_smd_coarse_and_angle.py`를 추가해 두 결과를 결합.
- 마지막 SMD 1번 시험 데이터:
  - TrayHome 중심 XYZ: `[-633.340, -170.954, -49.078] mm`.
  - TrayHome 6프레임 위치 편차 최대 0.029 mm.
  - 근접 미세각: canonical +2.003°, Base 장축 87.636°.
  - OBB 신뢰도 중앙값 0.9708, 각도 MAD 0.2356°, 8/8 인라이어.
  - 결합 후 Tool 보정과 Base X/Y 보정을 적용한 20 mm 호버 TCP: 약 `[-632.397, -169.586, -29.078]`.
- 실제 로봇에서 안전 높이 회전 87.636° → XY 이동 → 20 mm 호버까지 완료.

## 통합 트레이 화면 작업

- 카메라, FAIRINO 상태 서버, Unity Endpoint, 트레이 통합 비전을 최소 실행 명령 중심으로 정리하고 `/home/juchan-yoon/명령어.txt`를 최신화.
- 검출기 디버그 영상과 최종 합성 영상이 같은 토픽을 중복 발행하던 문제를 분리:
  - 최종 화면: `/vision/tray/detections_image/compressed`
  - 검출기 디버그: `/vision/tray/detector_debug_image/compressed`
- 의도한 3상태 화면 로직을 구현:
  - TrayHome 정지: 실시간 전체 트레이와 섹션.
  - 이동 중: 마지막 정상 TrayHome 전체 화면 고정 + 우측 상단 실시간 인셋.
  - 근접 위치 정지: 실시간 근접 화면만 표시하고 트레이 섹션 비활성화.
- TrayHome을 떠난 뒤 어긋난 homography 프레임이 고정되지 않도록 `at_trayhome && robot_motion_done`일 때만 전체 화면 캐시를 갱신.
- 근접 정지 시 트레이 섹션이 사라지는 로직은 적용했으나, 실제 재시험에서 이동 중 우측 상단 인셋이 다시 나타나지 않음. 인셋 문제는 미완료이며 다음 작업에서 재현·수정 필요.

## Unity·저장소 작업

- `ros2-ai-amr-repo3` main 브랜치 README의 9번 상태 다이어그램 이미지를 AutoPilot 폴더의 최신 이미지로 교체 후 푸시.
- Robot Final 이미지의 내부 색상 수정본만 다시 교체해 추가 푸시.
- `ros2-ai-cobot-repo1`의 `fr5-robot-control-full` 브랜치에 당일 작업 전체를 최신화해 커밋·푸시.
- Unity 디지털 트윈이 카메라 기반 부품 위치와 클래스별 수량을 구독하고, 실제 픽업 후 수량 감소/부품 사라짐을 반영할 수 있도록 트레이 비전 출력 연동을 포함.

## 마지막 실행의 중요 안전 상태

- SMD 1번은 회전 정렬 후 20 mm 호버까지 정상 완료.
- 사용자가 파지 높이까지 요청하여 저장된 절대 파지 Z `-53.153 mm`로 Base-Z 단일 하강 명령을 보냄.
- 해당 하강 클라이언트가 완료 로그를 반환하지 않았고 `/nonrt_state_data`도 응답하지 않아 실제 최종 Z와 `robot_motion_done`을 확인하지 못함.
- 그리퍼 닫힘 12 명령은 보내지 않았음.
- 응답 대기 중이던 `move_object_approach.py` 프로세스 PID 1227576의 중단을 시도했으나 사용자에 의해 도구 실행이 중단되어 실제 종료 여부 미확인.
- 따라서 작업 종료 시 로봇의 실제 위치·상태는 **미확인**으로 기록하며, 내일 절대로 바로 이동/그리퍼 명령을 보내지 말고 아래 시작 절차를 먼저 수행해야 함.

## 다음 작업 시작점

1. 물리 로봇 주변을 확인하고 비상정지 가능 상태를 확보한다.
2. `pgrep -af 'move_object_approach|fr_command_server'`로 PID 1227576 잔존 여부를 확인한다.
3. `/nonrt_state_data` 수신 복구 여부와 TCP XYZ, ABC, `robot_motion_done`, Tool1/User0, AUTO, 경보, 그리퍼 위치를 확인한다.
4. 상태 토픽이 없으면 로봇을 움직이지 말고 FAIRINO 상태 서버/컨트롤러 연결부터 복구한다.
5. 로봇이 SMD 파지 높이에 있는지 육안과 TCP로 함께 확인한다. 부품 접촉 중일 수 있으므로 XY 이동이나 회전을 금지한다.
6. 안전이 확인된 경우에만 그리퍼 12 닫힘 여부를 별도로 결정하고, 이후 5 mm 저속 수직 상승으로 파지를 검증한다.
7. SMD 픽업 검증 후 이동 중 인셋 미표시 문제를 재현한다. 렌더러가 `robot_motion_done` 전환을 실제로 수신하는지, 캐시 프레임이 생성됐는지 확인한다.
8. 인셋 수정 후 전체 흐름을 TrayHome 전체샷 → 이동 중 고정+인셋 → 근접 정지 전체화면 → 미세각 → 회전 → 20 mm 호버 순서로 다시 검증한다.

## 주요 변경·생성 파일

- `vision_assembly/config/part_gripper_recipes.json`
- `vision_assembly/scripts/safe_part_pick.py`
- `vision_assembly/run_safe_part_pick.sh`
- `vision_assembly/SAFE_PICK_WORKFLOW.md`
- `vision_assembly/scripts/capture_smd_close_target.py`
- `vision_assembly/scripts/merge_smd_coarse_and_angle.py`
- `vision_assembly/scripts/detect_smd_section.py`
- `vision_assembly/scripts/evaluate_smd_obb_live.py`
- `vision_assembly/config/smd_section_view.json`
- `vision_assembly/models/smd_obb/pilot_02/weights/best.pt`
- `vision_assembly/scripts/render_tray_live.py`
- `vision_assembly/scripts/view_tray_sections.py`
- `vision_assembly/run_tray_merged_detection.sh`
- `vision_assembly/data/smd_1_trayhome_coarse.json`
- `vision_assembly/data/smd_1_close_angle.json`
- `vision_assembly/data/smd_1_merged_target.json`
